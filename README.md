# KP_PM02
# 📚 50 Новых Тем Курсовых Проектов по ПМ.02  
**«Участие в интеграции программных модулей»**  
*(Java + PostgreSQL, desktop-приложения, без мобильных решений)*

> Каждая включает **минимум 2 интегрированных модуля**, **нормализованную БД (PostgreSQL)** и **реализацию на Java (Swing/JavaFX)**.
---

## 📖 Оглавление

### 🔹 Темы 1–10
- [Тема 1. Экоцентр: приём и переработка вторсырья](#-тема-1-экоцентр-приём-и-переработка-вторсырья)
- [Тема 2. Волонтёрский координационный центр](#-тема-2-волонтёрский-координационный-центр)
- [Тема 3. Система учёта гуманитарной помощи](#-тема-3-система-учёта-гуманитарной-помощи)
- [Тема 4. Цифровая библиотека редких изданий](#-тема-4-цифровая-библиотека-редких-изданий)
- [Тема 5. Платформа наставничества](#-тема-5-платформа-наставничества)
- [Тема 6. Учёт трудоустройства выпускников](#-тема-6-учёт-трудоустройства-выпускников)
- [Тема 7. Мониторинг качества воздуха в учреждениях](#-тема-7-мониторинг-качества-воздуха-в-учреждениях)
- [Тема 8. Экологическая тропа](#-тема-8-экологическая-тропа)
- [Тема 9. Учёт стипендий и соцподдержки студентов](#-тема-9-учёт-стипендий-и-соцподдержки-студентов)
- [Тема 10. Цифровое гражданское участие](#-тема-10-цифровое-гражданское-участие)

### 🔹 Темы 11–20
- [Тема 11. Учёт инвентаря в школьных мастерских](#-тема-11-учёт-инвентаря-в-школьных-мастерских)
- [Тема 12. Полевые экологические исследования](#-тема-12-полевые-экологические-исследования)
- [Тема 13. Анализ карьерных траекторий](#-тема-13-анализ-карьерных-траекторий)
- [Тема 14. Индивидуальный учебный план (ИУП)](#-тема-14-индивидуальный-учебный-план-иуп)
- [Тема 15. Организация школьных дебатов](#-тема-15-организация-школьных-дебатов)
- [Тема 16. Учёт волонтёрских часов](#-тема-16-учёт-волонтёрских-часов)
- [Тема 17. Культурная карта для подростков](#-тема-17-культурная-карта-для-подростков)
- [Тема 18. Обратная связь по школьному питанию](#-тема-18-обратная-связь-по-школьному-питанию)
- [Тема 19. Интеграция школьных мероприятий с родительским порталом](#-тема-19-интеграция-школьных-мероприятий-с-родительским-порталом)
- [Тема 20. Учёт дронов в STEM-проектах](#-тема-20-учёт-дронов-в-stem-проектах)

### 🔹 Темы 21–30
- [Тема 21. Аудиогиды по локальным достопримечательностям](#-тема-21-аудиогиды-по-локальным-достопримечательностям)
- [Тема 22. Архив устной истории](#-тема-22-архив-устной-истории)
- [Тема 23. Управление STEM-проектами](#-тема-23-управление-stem-проектами)
- [Тема 24. Контроль домашних заданий](#-тема-24-контроль-домашних-заданий)
- [Тема 25. Учёт научно-технического творчества учащихся](#-тема-25-учёт-научно-технического-творчества-учащихся)
- [Тема 26. Сервис бронирования учебных классов и оборудования](#-тема-26-сервис-бронирования-учебных-классов-и-оборудования)
- [Тема 27. Цифровое портфолио педагога](#-тема-27-цифровое-портфолио-педагога)
- [Тема 28. Система отчётности по внеурочной деятельности](#-тема-28-сис-тема-отчётности-по-внеурочной-деятельности)
- [Тема 29. Интерактивный календарь школьных событий](#-тема-29-интерактивный-календарь-школьных-событий)
- [Тема 30. Модуль мониторинга энергопотребления в образовательной организации](#-тема-30-модуль-мониторинга-энергопотребления-в-образовательной-организации)

### 🔹 Темы 31–40
- [Тема 31. Система подбора наставников по интересам и компетенциям](#-тема-31-сис-тема-подбора-наставников-по-интересам-и-компетенциям)
- [Тема 32. Цифровая копилка школьных традиций и ритуалов](#-тема-32-цифровая-копилка-школьных-традиций-и-ритуалов)
- [Тема 33. Платформа для проведения онлайн-олимпиад](#-тема-33-платформа-для-проведения-онлайн-олимпиад)
- [Тема 34. Система учёта посещаемости культурных учреждений учащимися](#-тема-34-сис-тема-учёта-посещаемости-культурных-учреждений-учащимися)
- [Тема 35. Система ориентирования на местности с AR-подсказками (desktop-имитация)](#-тема-35-сис-тема-ориентирования-на-местности-с-ar-подсказками-desktop-имитация)
- [Тема 36. Обратная связь по качеству онлайн-курсов](#-тема-36-обратная-связь-по-качеству-онлайн-курсов)
- [Тема 37. Платформа для совместной разработки open-source учебных проектов](#-тема-37-платформа-для-совместной-разработки-open-source-учебных-проектов)
- [Тема 38. Интеграция школьной системы с порталом госуслуг](#-тема-38-интеграция-школьной-системы-с-порталом-госуслуг)
- [Тема 39. Геймификация школьного самоуправления](#-тема-39-геймификация-школьного-самоуправления)
- [Тема 40. Учёт инклюзивных образовательных практик](#-тема-40-учёт-инклюзивных-образовательных-практик)

### 🔹 Темы 41–50
- [Тема 41. Центр помощи бездомным животным](#-тема-41-центр-помощи-бездомным-животным)
- [Тема 42. Система отслеживания миграции диких животных](#-тема-42-сис-тема-отслеживания-миграции-диких-животных)
- [Тема 43. Школа ментальной арифметики IQ007](#-тема-43-школа-ментальной-арифметики-iq007)
- [Тема 44. Студия звукозаписи](#-тема-44-студия-звукозаписи)
- [Тема 45. Химчистка](#-тема-45-химчистка)
- [Тема 46. Мастерская по ремонту одежды](#-тема-46-мастерская-по-ремонту-одежды)
- [Тема 47. Производство светильников «Светлячок» (продукция)](#-тема-47-производство-светильников-светлячок-подсистема-работы-с-продукцией)
- [Тема 48. Техническое обслуживание станков](#-тема-48-техническое-обслуживание-станков)
- [Тема 49. Учёт личных автомобилей](#-тема-49-учёт-личных-автомобилей)
- [Тема 50. Конструктор экзаменационных билетов](#-тема-50-конструктор-экзаменационных-билетов)

---

## 🧩 Требования к реализации (единые для всех тем)
- **Язык**: Java (≥11), desktop (Swing / JavaFX)  
- **СУБД**: PostgreSQL (≥12)  
- **Архитектура**: ≥2 взаимосвязанных модуля (например: *учёт → отчётность*, *регистрация → анализ*)  
- **Безопасность**: хеширование паролей, ограничение прав пользователей  
- **Документация**: отчёт в Word, `README.md` с инструкцией по развёртыванию  

### 🔹 Тема 1. **Экоцентр: приём и переработка вторсырья**

#### 📌 Описание
Экоцентр принимает макулатуру, пластик, стекло, металл от населения и организаций. После приёма сырьё сортируется, прессуется и передаётся на переработку. Система должна автоматизировать учёт поступления, хранения, переработки и отчётов перед экологическими службами.

#### 🧩 Сущности
- Поставщик (физ/юрлицо, контакты)  
- Вид вторсырья (пластик ПЭТ, макулатура А4 и т.д.)  
- Приёмная партия (дата, вес, поставщик, вид)  
- Склад (ячейка, текущий остаток)  
- Этап переработки (сортировка → прессовка → отгрузка)  
- Отгрузка переработчику (дата, объём, получатель)

#### 📊 Таблицы (рекомендуемые)
| Таблица | Поля (тип) |
|--------|-------------|
| `suppliers` | `id (PK)`, `name (TEXT)`, `type (CHAR: 'F'/'L')`, `phone (TEXT)`, `email (TEXT)` |
| `waste_types` | `id (PK)`, `name (TEXT)`, `density_kg_per_m3 (NUMERIC)`, `recyclable (BOOL)` |
| `receipts` | `id (PK)`, `supplier_id (FK)`, `waste_type_id (FK)`, `weight_kg (NUMERIC)`, `datetime (TIMESTAMP)`, `operator_id (FK)` |
| `storage_cells` | `id (PK)`, `code (TEXT UNIQUE)`, `max_capacity_kg (NUMERIC)`, `current_load_kg (NUMERIC)` |
| `processing_stages` | `id (PK)`, `receipt_id (FK)`, `stage (ENUM: 'sorting','pressing','shipping')`, `start_time`, `end_time`, `status (ENUM)` |
| `shipments` | `id (PK)`, `processed_batch_id (FK → processing_stages.id где stage='shipping')`, `recipient (TEXT)`, `datetime`, `weight_kg`, `transport_doc (TEXT)` |
| `users` | `id (PK)`, `login (TEXT UNIQUE)`, `password_hash (TEXT)`, `role (ENUM: 'admin','operator','viewer')` |

#### ⚙️ Интеграция модулей
- **Модуль 1. Приём** → формирует запись в `receipts`, обновляет `storage_cells`.  
- **Модуль 2. Переработка** → берёт данные из `receipts`, создаёт цепочку в `processing_stages`, завершает в `shipments`.  
- → **Единый отчёт**: «Оборот по видам за месяц» (`receipts` + `shipments`, с разницей — текущий остаток).

#### ✅ Для «отлично»
- Валидация: нельзя отгрузить больше, чем есть на складе.  
- Журнал операций (`audit_log`: кто, когда, что изменил).  
- График динамики приёма (встроенный `ChartPanel` в JavaFX/Swing).

---

### 🔹 Тема 2. **Волонтёрский координационный центр**

#### 📌 Описание
Организация координирует волонтёров для мероприятий (уборка парков, помощь в приютах и т.д.). Нужна система: регистрация волонтёров → заявки от заказчиков → формирование бригад → учёт отработанных часов → формирование сертификатов.

#### 🧩 Сущности
- Волонтёр (ФИО, возраст, навыки, контакты)  
- Навык (например: «работа с детьми», «медицинская помощь»)  
- Мероприятие (название, дата, место, организатор, требуемые навыки и число человек)  
- Бригада (мероприятие + список волонтёров)  
- Отчёт о мероприятии (фото, часы, подтверждение куратора)  

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `volunteers` | `id`, `fio`, `birth_year`, `phone`, `email`, `registration_date` |
| `skills` | `id`, `name` |
| `volunteer_skills` | `volunteer_id (FK)`, `skill_id (FK)` *(многие-ко-многим)* |
| `events` | `id`, `title`, `description`, `datetime`, `location`, `organizer`, `required_volunteers`, `status (planned/active/done/cancelled)` |
| `event_required_skills` | `event_id`, `skill_id`, `count_needed` |
| `brigades` | `id`, `event_id`, `volunteer_id`, `role (leader/member)` |
| `event_reports` | `id`, `event_id`, `volunteer_id`, `hours_worked`, `confirmed (BOOL)`, `notes`, `report_date` |
| `certificates` | `id`, `volunteer_id`, `event_id`, `issue_date`, `hash (для подлинности)` |

#### ⚙️ Интеграция
- **Модуль 1. Подбор волонтёров** → по навыкам и доступности → формирует `brigades`.  
- **Модуль 2. Учёт и сертификация** → на основе `event_reports` → генерирует `certificates`.  
- → Интеграция: нельзя выдать сертификат без подтверждённого отчёта.

#### ✅ Для «отлично»
- Рейтинг волонтёра (сумма часов + отзывы).  
- Экспорт сертификата в PDF (iText).  
- Ограничение: куратор (роль `coordinator`) может подтверждать отчёты, волонтёр — нет.

---

### 🔹 Тема 3. **Система учёта гуманитарной помощи**

#### 📌 Описание
Центр помощи (например, при ЧС или для беженцев) принимает поступления (еда, одежда, лекарства), хранит и распределяет по заявкам от получателей (семьи, лагеря). Важно отслеживать: что пришло, кому отдано, остатки, сроки годности.

#### 🧩 Сущности
- Помощь (категория: продукты/одежда/лекарства; наименование; срок годности)  
- Поступление (дата, поставщик, список позиций с количеством)  
- Получатель (семья/лагерь, адрес, контакты, состав)  
- Заявка на помощь (получатель, нуждаемость, статус)  
- Выдача (заявка, позиции, дата, подпись)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `aid_categories` | `id`, `name` |
| `aid_items` | `id`, `name`, `category_id`, `shelf_life_days (INT)`, `unit (шт/кг/упак)` |
| `donations` | `id`, `donor_name`, `datetime`, `status` |
| `donation_items` | `id`, `donation_id`, `item_id`, `quantity`, `expiry_date (DATE)` |
| `recipients` | `id`, `type ('family'/'camp')`, `name`, `address`, `contact_person`, `phone` |
| `requests` | `id`, `recipient_id`, `datetime`, `status ('new','approved','fulfilled','rejected')`, `notes` |
| `request_items` | `request_id`, `item_id`, `quantity_requested`, `quantity_fulfilled` |
| `distributions` | `id`, `request_id`, `datetime`, `operator_id`, `signature_scan_path` |

#### ⚙️ Интеграция
- **Модуль 1. Приём** → добавляет в `donation_items`, обновляет остатки (можно через `VIEW` `current_stock`).  
- **Модуль 2. Распределение** → проверяет остатки, резервирует, создаёт `distributions`.  
- → Автоматический контроль: нельзя выдать просроченное или отсутствующее.

#### ✅ Для «отлично»
- Предупреждение о скором истечении срока (`SELECT * FROM donation_items WHERE expiry_date <= NOW() + INTERVAL '3 days'`).  
- Отчёт: «Потери из-за просрочки».

---

### 🔹 Тема 4. **Цифровая библиотека редких изданий**

#### 📌 Описание
Библиотека хранит уникальные книги, рукописи, газеты. Доступ — строго по записи, с ограничением срока и места (только читальный зал). Нужен учёт: фонды → выдача → возврат → повреждения.

#### 🧩 Сущности
- Издание (шифр, название, год, тип: книга/рукопись/газета, состояние)  
- Автор  
- Читатель (ФИО, тип: исследователь/студент, организация)  
- Заказ на выдачу (читатель, издание, цель, дата, статус)  
- Сессия чтения (дата, время, место, факт выдачи/возврата, повреждения)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `authors` | `id`, `fio`, `birth_year`, `death_year` |
| `items` | `id`, `code (UNIQUE)`, `title`, `year`, `type`, `condition (ENUM: 'excellent','good','fair','poor')`, `location_shelf` |
| `item_authors` | `item_id`, `author_id` |
| `readers` | `id`, `fio`, `type`, `org`, `passport_data`, `registration_date` |
| `requests` | `id`, `reader_id`, `item_id`, `purpose (TEXT)`, `request_date`, `status ('pending','approved','denied','completed')` |
| `reading_sessions` | `id`, `request_id`, `start_datetime`, `end_datetime`, `hall_number`, `issued (BOOL)`, `returned (BOOL)`, `damage_note (TEXT)` |

#### ⚙️ Интеграция
- **Модуль 1. Приём заявок** → согласование (админ), резервирование издания.  
- **Модуль 2. Проведение сессии** → отметка выдачи/возврата → обновление статуса заявки.  
- → Нельзя выдать одно издание двум читателям одновременно.

#### ✅ Для «отлично»
- Оповещение за 1 день до окончания срока.  
- Статистика: «Топ-10 запрашиваемых изданий».

---

### 🔹 Тема 5. **Платформа наставничества**

#### 📌 Описание
Система подбора наставника (опытный специалист) и подопечного (студент/начинающий). Определяются цели, составляется план, отслеживается прогресс, проводятся встречи.

#### 🧩 Сущности
- Наставник (ФИО, компетенции, доступность)  
- Подопечный (ФИО, цели, текущий уровень)  
- Направление (IT, педагогика, инженерия)  
- Пара «наставник—подопечный»  
- План развития (этапы, сроки, цели)  
- Встреча (дата, тема, выполненные задачи, оценка)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `mentors` | `id`, `fio`, `email`, `specialization`, `available (BOOL)` |
| `mentees` | `id`, `fio`, `email`, `goals (TEXT)`, `current_level (1-5)` |
| `directions` | `id`, `name` |
| `mentor_directions` | `mentor_id`, `direction_id` |
| `pairs` | `id`, `mentor_id`, `mentee_id`, `start_date`, `status ('active','paused','completed')` |
| `development_plans` | `id`, `pair_id`, `title`, `description`, `deadline` |
| `meetings` | `id`, `pair_id`, `datetime`, `topic`, `tasks_done`, `mentor_rating (1-5)`, `mentee_rating (1-5)` |

#### ⚙️ Интеграция
- **Модуль 1. Подбор и формирование пары** → на основе направлений и доступности.  
- **Модуль 2. Отслеживание прогресса** → на основе `meetings` и `development_plans`.  
- → Отчёт: «Эффективность наставничества» (ср. рейтинг, % завершённых планов).

---

*(Продолжаю далее — чтобы не перегружать, приведу ещё 10 тем подробно, оставшиеся 35 — кратко, но с теми же разделами. Если нужно — могу сразу все 50 в таком формате, но это ~25–30 тыс. знаков. Уточните — или продолжу здесь.)*

---

### 🔹 Тема 6. **Учёт трудоустройства выпускников**

#### 📌 Описание
Колледж/вуз отслеживает, где и кем работают выпускники — для анализа качества подготовки и корректировки образовательных программ.

#### 🧩 Сущности
- Выпускник (ФИО, год выпуска, специальность)  
- Работодатель (название, отрасль, город)  
- Трудоустройство (выпускник, работодатель, должность, дата приёма, текущий статус)  
- Опрос (через 6/12 месяцев: удовлетворённость, соответствие специальности)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `graduates` | `id`, `fio`, `graduation_year`, `specialty`, `contact_phone`, `email` |
| `employers` | `id`, `name`, `industry`, `city`, `contact_person` |
| `placements` | `id`, `graduate_id`, `employer_id`, `position`, `hire_date`, `is_current (BOOL)`, `end_date (NULL if current)` |
| `surveys` | `id`, `placement_id`, `months_after (6/12/24)`, `salary`, `satisfaction (1-10)`, `relevance (1-10)`, `comments` |

#### ⚙️ Интеграция
- **Модуль 1. Учёт трудоустройства** → ввод данных HR или самого выпускника.  
- **Модуль 2. Аналитика** → генерация отчётов: «% трудоустроенных по специальности», «динамика зарплат».

#### ✅ Для «отлично»
- Автоматическое напоминание о прохождении опроса (через `ScheduledExecutorService`).  
- Экспорт в Excel (Apache POI).

---

### 🔹 Тема 7. **Мониторинг качества воздуха в учреждениях**

#### 📌 Описание
В кабинетах установлены датчики (условно — данные имитируются или вводятся вручную): CO₂, температура, влажность. Система собирает данные, сравнивает с нормативами, уведомляет при превышениях.

#### 🧩 Сущности
- Помещение (номер, тип: кабинет/актовый/столовая)  
- Датчик (ID, тип, место установки)  
- Измерение (время, CO₂, t, влажность)  
- Норматив (по типу помещения: макс CO₂ = 800 ppm)  
- Событие (превышение → дата, параметр, значение, статус устранения)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `rooms` | `id`, `number`, `type`, `building` |
| `sensors` | `id`, `room_id`, `type ('co2','temp','humidity')`, `location_note` |
| `measurements` | `id`, `sensor_id`, `datetime`, `value (NUMERIC)` |
| `standards` | `room_type`, `param`, `max_value`, `min_value` |
| `alerts` | `id`, `room_id`, `param`, `value`, `datetime`, `resolved (BOOL)`, `resolved_by`, `resolution_time` |

#### ⚙️ Интеграция
- **Модуль 1. Приём данных** (ввод вручную или имитация потока).  
- **Модуль 2. Мониторинг и оповещения** → проверяет `measurements` против `standards` → создаёт `alerts`.  
- → Дашборд: «Текущее состояние по зданию».

#### ✅ Для «отлично»
- Графики динамики (JavaFX `LineChart`).  
- Экспорт алертов в отчёт за день.

---

### 🔹 Тема 8. **Экологическая тропа**

#### 📌 Описание
Образовательная тропа в парке/лесу с интерактивными точками (стенды, QR-коды). Система: учёт групп → прохождение точек → тестирование знаний → отчёт педагогу.

#### 🧩 Сущности
- Точка тропы (номер, название, описание, координаты)  
- Группа (школа, класс, педагог, дата посещения)  
- Посещение точки (группа, точка, время, ответы на вопросы)  
- Вопрос (текст, правильный ответ, баллы)  
- Результат (группа, суммарный балл, дата)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `trail_points` | `id`, `number`, `title`, `description`, `lat`, `lon` |
| `questions` | `id`, `point_id`, `text`, `correct_answer`, `points` |
| `groups` | `id`, `school`, `class`, `teacher_fio`, `visit_date` |
| `point_visits` | `id`, `group_id`, `point_id`, `visit_time`, `answers_json (TEXT)` |
| `results` | `id`, `group_id`, `total_points`, `max_possible`, `completion_time` |

#### ⚙️ Интеграция
- **Модуль 1. Регистрация групп и прохождение** → создание записей в `point_visits`.  
- **Модуль 2. Проверка и отчёт** → на основе `answers_json` → расчёт `results`.  
- → Нельзя завершить тропу, не посетив все точки.

---

### 🔹 Тема 9. **Учёт стипендий и соцподдержки студентов**

#### 📌 Описание
Автоматизация начисления академических, социальных и именных стипендий: сбор оснований → расчёт → формирование ведомостей → выгрузка в бухгалтерию.

#### 🧩 Сущности
- Студент (ФИО, группа, средний балл, льготы)  
- Тип стипендии (академическая, социальная, губернаторская)  
- Основание (справка, приказ, конкурс)  
- Начисление (студент, тип, сумма, месяц, статус)  
- Ведомость (месяц, статус, дата формирования)

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `students` | `id`, `fio`, `group_code`, `avg_grade`, `has_social_status (BOOL)` |
| `scholarship_types` | `id`, `name`, `base_amount`, `requires_docs (BOOL)` |
| `grounds` | `id`, `student_id`, `type_id`, `doc_type`, `issue_date`, `valid_until` |
| `accruals` | `id`, `student_id`, `type_id`, `amount`, `for_month (DATE)`, `status ('calculated','paid','cancelled')` |
| `payrolls` | `id`, `for_month`, `created_at`, `status`, `file_path (PDF)` |

#### ⚙️ Интеграция
- **Модуль 1. Расчёт** → по правилам (например: академическая — если `avg_grade ≥ 4.5`).  
- **Модуль 2. Формирование ведомостей** → агрегация `accruals` → генерация PDF.  
- → Контроль: дублирование начислений в один месяц запрещено.

---

### 🔹 Тема 10. **Цифровое гражданское участие**

#### 📌 Описание
Платформа для подачи инициатив жителями (благоустройство, безопасность), обсуждения, голосования и отслеживания реализации.

#### 🧩 Сущности
- Инициатива (автор, тема, описание, статус)  
- Этап (подача → обсуждение → голосование → реализация)  
- Голос (пользователь, инициатива, выбор, дата)  
- Комментарий  
- Ответ администрации

#### 📊 Таблицы
| Таблица | Поля |
|--------|------|
| `initiatives` | `id`, `author_id`, `title`, `description`, `status`, `created_at` |
| `stages` | `id`, `initiative_id`, `stage_name`, `start_date`, `end_date`, `result` |
| `votes` | `id`, `user_id`, `initiative_id`, `choice ('for','against')`, `datetime` |
| `comments` | `id`, `initiative_id`, `user_id`, `text`, `datetime` |
| `official_responses` | `id`, `initiative_id`, `text`, `author_position`, `datetime` |

#### ⚙️ Интеграция
- **Модуль 1. Подача и обсуждение** → `initiatives` + `comments`.  
- **Модуль 2. Голосование и итоги** → `votes` → автоматический переход этапа при наборе кворума.

---

### 🔹 Тема 11. **Учёт инвентаря в школьных мастерских**

#### 📌 Описание  
Школьные мастерские (робототехника, столярная, электротехническая) оснащены дорогим оборудованием: 3D-принтеры, фрезерные станки, паяльные станции, наборы конструкторов. Чтобы избежать потерь, поломок и конфликтов, требуется система:  
- учёта оборудования (инвентарный номер, состояние, место хранения),  
- бронирования (кто, когда, на какое занятие),  
- фиксации неисправностей и ремонта,  
- списания по износу.

#### 🧩 Сущности  
- **Оборудование**: инвентарный №, наименование, тип (станок/инструмент/расходник), состояние, дата покупки, гарантия до, место хранения (шкаф/стол/стенд).  
- **Тип оборудования**: «3D-принтер», «Паяльная станция», «Набор Arduino» — для группировки.  
- **Пользователь**: учитель/студент (роль, ФИО, группа/предмет).  
- **Бронирование**: оборудование, пользователь, дата и время, цель (урок/проект), статус (ожидает/одобрено/отменено).  
- **Ремонт/неисправность**: оборудование, дата выявления, описание, статус (в работе/готово), исполнитель.  
- **Списание**: оборудование, дата, причина, приказ №.

#### 📊 Таблицы (PostgreSQL)

| Таблица | Поля (`тип`, комментарий) |
|--------|----------------------------|
| `equipment_types` | `id (SERIAL PK)`, `name (TEXT NOT NULL)` |
| `equipment` | `id (SERIAL PK)`, `inv_number (TEXT UNIQUE NOT NULL)`, `name (TEXT)`, `type_id (INT FK → equipment_types.id)`, `condition (ENUM: 'new','good','fair','broken')`, `purchase_date (DATE)`, `warranty_until (DATE)`, `location (TEXT)`, `is_available (BOOL DEFAULT true)` |
| `users` | `id (SERIAL PK)`, `fio (TEXT)`, `role (ENUM: 'teacher','student')`, `group_or_subject (TEXT)` |
| `bookings` | `id (SERIAL PK)`, `equipment_id (INT FK)`, `user_id (INT FK)`, `start_time (TIMESTAMP)`, `end_time (TIMESTAMP)`, `purpose (TEXT)`, `status (ENUM: 'pending','approved','rejected','completed')`, `created_at (TIMESTAMP DEFAULT NOW())` |
| `repairs` | `id (SERIAL PK)`, `equipment_id (INT FK)`, `reported_by (INT FK → users.id)`, `description (TEXT)`, `reported_at (TIMESTAMP)`, `status (ENUM: 'reported','in_progress','completed')`, `completed_at (TIMESTAMP NULL)`, `repairer (TEXT)` |
| `disposals` | `id (SERIAL PK)`, `equipment_id (INT FK)`, `disposal_date (DATE)`, `reason (TEXT)`, `order_number (TEXT)`, `approved_by (TEXT)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Учёт и бронирование**  
  → При создании брони проверяется `is_available = true` и пересечение по времени.  
  → При подтверждении брони — `is_available = false`.  
- **Модуль 2. Обслуживание и списание**  
  → При создании записи в `repairs` автоматически ставится `equipment.is_available = false`.  
  → После завершения ремонта (`status = 'completed'`) — `is_available = true` (если не списано).  
  → Списание → удаление из оборота (`is_available = false`, `condition = 'scrapped'`).  
- **Сквозная логика**:  
  → Нельзя забронировать оборудование в ремонте или списанное.  
  → Отчёт: «Загрузка оборудования за месяц» (бронирования + простои в ремонте).

#### ✅ Для «отлично»  
- Валидация: `end_time > start_time`, пересечение бронирований запрещено.  
- Напоминание за 1 день до окончания брони (в GUI — выделение жёлтым).  
- Журнал аудита (`audit_log`: кто изменил состояние оборудования и когда).  
- График: «Доля времени в работе / ремонте / простое» (JavaFX `PieChart`).

---

### 🔹 Тема 12. **Полевые экологические исследования**

#### 📌 Описание  
Школьные/студенческие экологические отряды проводят исследования на местности: замеряют параметры воды, почвы, воздуха; берут пробы; фиксируют биоразнообразие. Раньше — в бумажных блокнотах. Требуется система для:  
- регистрации исследований,  
- ввода данных (в поле — через desktop-ноутбук или позже),  
- привязки к GPS-координатам,  
- формирования отчётов и карт загрязнений.

#### 🧩 Сущности  
- **Исследователь**: ФИО, отряд, контакты.  
- **Локация**: название, координаты (широта/долгота), тип (река/лес/поляна).  
- **Объект исследования**: вода, почва, воздух, биота.  
- **Параметр**: pH, мутность, содержание нитратов, температура и т.д.  
- **Проба**: локация, объект, дата, исследователь, параметры (многие-ко-многим).  
- **Анализ**: проба, лаборатория, результаты, заключение.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `researchers` | `id (PK)`, `fio (TEXT)`, `team_name (TEXT)`, `phone (TEXT)` |
| `locations` | `id (PK)`, `name (TEXT)`, `lat (NUMERIC(9,6))`, `lon (NUMERIC(9,6))`, `type (TEXT)`, `description (TEXT)` |
| `objects` | `id (PK)`, `name (TEXT) — 'вода','почва','воздух'` |
| `parameters` | `id (PK)`, `name (TEXT)`, `unit (TEXT — 'pH','мг/л','°C')`, `object_id (FK)` |
| `samples` | `id (PK)`, `location_id (FK)`, `object_id (FK)`, `datetime (TIMESTAMP)`, `researcher_id (FK)`, `notes (TEXT)` |
| `sample_parameters` | `sample_id (FK)`, `parameter_id (FK)`, `value (NUMERIC)`, `measurement_method (TEXT)` |
| `analyses` | `id (PK)`, `sample_id (FK)`, `lab_name (TEXT)`, `conclusion (TEXT)`, `report_date (DATE)`, `file_path (TEXT)` |

> 💡 *PostGIS не обязателен — координаты хранятся как `NUMERIC`. Но можно добавить `POINT` при наличии расширения.*

#### ⚙️ Интеграция модулей  
- **Модуль 1. Сбор данных** → ввод пробы + параметров → сохранение в `samples` и `sample_parameters`.  
- **Модуль 2. Анализ и отчётность** → привязка лабораторного заключения → генерация сводки по локации.  
- → Отчёт: «Динамика pH воды в реке Х за 2024–2025 гг.» (`samples` + `sample_parameters` + `locations`).

#### ✅ Для «отлично»  
- Валидация диапазонов: `pH` ∈ [0,14], `температура воды` ∈ [-5, 40].  
- Фильтр по локации на карте (в GUI — таблица + кнопка «Показать на карте» → открытие в браузере: `https://yandex.ru/maps/?text=55.75396,37.620393`).  
- Экспорт в CSV для анализа в Excel/Google Таблицы.

---

### 🔹 Тема 13. **Анализ карьерных траекторий**

#### 📌 Описание  
Система помогает студентам и кураторам оценивать, насколько их компетенции соответствуют требованиям рынка труда.  
Студент заполняет профиль (навыки, сертификаты, проекты) → система сопоставляет с вакансиями (вручную внесёнными или из открытых источников) → даёт рекомендации по обучению.

#### 🧩 Сущности  
- **Студент**: ФИО, специальность, курс, email.  
- **Компетенция**: «Java», «Работа с PostgreSQL», «Командная работа».  
- **Уровень владения**: начинающий / уверенный / эксперт (1–3).  
- **Вакансия**: название, работодатель, описание, требуемые компетенции, зарплата.  
- **Соответствие**: студент + вакансия → % совпадения.  
- **Рекомендация**: курс/MOOC/практикa для закрытия пробелов.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `students` | `id (PK)`, `fio (TEXT)`, `specialty (TEXT)`, `course (INT)`, `email (TEXT)` |
| `competencies` | `id (PK)`, `name (TEXT UNIQUE)`, `category (TEXT — 'hard','soft','domain')` |
| `student_competencies` | `student_id (FK)`, `competency_id (FK)`, `level (INT CHECK BETWEEN 1 AND 3)` |
| `vacancies` | `id (PK)`, `title (TEXT)`, `employer (TEXT)`, `description (TEXT)`, `salary_from (NUMERIC)`, `salary_to (NUMERIC)`, `posted_at (DATE)` |
| `vacancy_requirements` | `vacancy_id (FK)`, `competency_id (FK)`, `required_level (INT)` |
| `matches` | `id (PK)`, `student_id (FK)`, `vacancy_id (FK)`, `match_percent (NUMERIC)`, `calculated_at (TIMESTAMP)` |
| `recommendations` | `id (PK)`, `student_id (FK)`, `competency_id (FK)`, `course_name (TEXT)`, `link (TEXT)`, `priority (INT)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Формирование профиля** → ввод компетенций → сохранение в `student_competencies`.  
- **Модуль 2. Подбор и рекомендации** →  
  1. Для каждой вакансии считается `match_percent` = (совпадающие компетенции × вес) / всего требуемых.  
  2. Если < 70% → генерируются `recommendations`.  
- → Отчёт: «Топ-5 вакансий для студента» + «Что прокачать».

#### ✅ Для «отлично»  
- Автоматический пересчёт `match_percent` при изменении профиля.  
- Фильтры: по зарплате, работодателю, категории компетенций.  
- Диаграмма: «Радар компетенций студента» (JavaFX `RadarChart` — можно реализовать через `Polygon`).

---

### 🔹 Тема 14. **Индивидуальный учебный план (ИУП)**

#### 📌 Описание  
Для студентов с академической разницей, инвалидностью или ускоренным обучением разрабатывается ИУП: персональный график сдачи дисциплин, пересдач, практик. Система позволяет:  
- формировать ИУП на основе стандартного плана,  
- переносить сроки,  
- отмечать выполнение,  
- генерировать отчёты для комиссии.

#### 🧩 Сущности  
- **Студент**, **Группа**, **Специальность**  
- **Дисциплина**: код, название, семестр по стандарту.  
- **ИУП**: студент, дата утверждения, куратор.  
- **Пункт ИУП**: дисциплина, планируемая дата сдачи, форма (экзамен/зачёт/курсовой), статус.  
- **Попытка сдачи**: дата, оценка, преподаватель, замечания.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `students`, `groups`, `specialties` — стандартные |
| `disciplines` | `id (PK)`, `code (TEXT)`, `name (TEXT)`, `semester_std (INT)`, `specialty_id (FK)`, `control_form (TEXT)` |
| `iups` | `id (PK)`, `student_id (FK)`, `curator_id (FK → users.id)`, `approved_at (DATE)`, `status ('draft','approved','completed')` |
| `iup_items` | `id (PK)`, `iup_id (FK)`, `discipline_id (FK)`, `planned_date (DATE)`, `control_form (TEXT)`, `status ('planned','passed','failed','postponed')` |
| `attempts` | `id (PK)`, `iup_item_id (FK)`, `attempt_date (DATE)`, `grade (INT CHECK BETWEEN 2 AND 5)`, `teacher_fio (TEXT)`, `comments (TEXT)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Формирование ИУП** → копирование дисциплин из стандартного плана → редактирование дат.  
- **Модуль 2. Отслеживание прогресса** → ввод попыток → автоматическое обновление `iup_items.status`.  
- → Логика: если `grade ≥ 3` → `status = 'passed'`; иначе — `failed`, можно перенести дату.

#### ✅ Для «отлично»  
- Календарь в GUI (JavaFX `CalendarPicker` или табличный вид) с цветовой индикацией:  
  - 🟢 — сдано,  
  - 🟡 — через ≤7 дней,  
  - 🔴 — просрочено.  
- Генерация PDF-ИУП (iText) с подписями (место для ФИО).  
- Ограничение: нельзя утвердить ИУП без куратора.

---

### 🔹 Тема 15. **Организация школьных дебатов**

#### 📌 Описание  
Организация дебат-клуба: регистрация команд, жеребьёвка, проведение раундов, судейство, подсчёт баллов, формирование рейтинга. Подходит для олимпиадного движения.

#### 🧩 Сущности  
- **Команда**: школа, название, капитан, состав (ФИО участников).  
- **Участник** (отдельно — для персонального рейтинга).  
- **Тема дебатов**: формулировка, категория (политика, этика, наука).  
- **Раунд**: команда «за», команда «против», тема, судьи, дата/время.  
- **Судья**: ФИО, категория (учитель/студент/приглашённый эксперт).  
- **Протокол**: критерии (логика, риторика, этика), баллы, итог.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `teams` | `id (PK)`, `school (TEXT)`, `team_name (TEXT)`, `captain_fio (TEXT)` |
| `members` | `id (PK)`, `team_id (FK)`, `fio (TEXT)`, `role (TEXT — 'speaker1','speaker2','analyst')` |
| `topics` | `id (PK)`, `text (TEXT)`, `category (TEXT)` |
| `judges` | `id (PK)`, `fio (TEXT)`, `category (TEXT)`, `email (TEXT)` |
| `rounds` | `id (PK)`, `team_for_id (FK)`, `team_against_id (FK)`, `topic_id (FK)`, `datetime (TIMESTAMP)`, `venue (TEXT)`, `status ('scheduled','completed','cancelled')` |
| `round_judges` | `round_id (FK)`, `judge_id (FK)` *(многие-ко-многим)* |
| `protocols` | `id (PK)`, `round_id (FK)`, `judge_id (FK)`, `logic_for (INT 1-10)`, `rhetoric_for (INT)`, `ethics_for (INT)`, `logic_against (INT)`, `rhetoric_against (INT)`, `ethics_against (INT)`, `winner ('for','against','draw')` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Организация** → создание раунда + назначение судей → `rounds` + `round_judges`.  
- **Модуль 2. Судейство и итоги** → ввод протоколов → автоматический расчёт:  
  - средний балл по критериям,  
  - победитель раунда,  
  - общий рейтинг команды (`SELECT AVG(total_score) FROM protocols JOIN rounds...`).  
- → Отчёт: «Лучший оратор» (по сумме `rhetoric_*`).

#### ✅ Для «отлично»  
- Автоматическая жеребьёвка (рандом + баланс по рейтингу).  
- Валидация: один судья не может оценивать одну и ту же команду дважды подряд.  
- Экспорт протокола в печатную форму (Word через Apache POI).

---

### 🔹 Тема 16. **Учёт волонтёрских часов**

#### 📌 Описание  
Волонтёры участвуют в мероприятиях (уборка, помощь в приютах, фестивали). Организаторы фиксируют:  
- сколько часов отработано,  
- подтверждение куратора,  
- тип активности,  
- на основе этого — формируются сертификаты и личные отчёты.

#### 🧩 Сущности  
- **Волонтёр**: ФИО, дата рождения, контакты, дата регистрации.  
- **Мероприятие**: название, дата, место, организатор, тип (экология, соцподдержка).  
- **Участие**: волонтёр, мероприятие, фактические часы, подтверждено (да/нет), куратор.  
- **Сертификат**: номер, волонтёр, период, общие часы, дата выдачи, хеш для проверки.

#### 📊 Таблицы *(частично повторяет Тему 2, но фокус на часах и сертификатах)*

| Таблица | Поля |
|--------|------|
| `volunteers` | `id (PK)`, `fio (TEXT)`, `birth_date (DATE)`, `phone (TEXT)`, `email (TEXT)`, `registered_at (DATE)` |
| `events` | `id (PK)`, `title (TEXT)`, `datetime (TIMESTAMP)`, `location (TEXT)`, `type (TEXT)`, `organizer (TEXT)` |
| `participations` | `id (PK)`, `volunteer_id (FK)`, `event_id (FK)`, `hours_worked (NUMERIC(4,1))`, `confirmed (BOOL DEFAULT false)`, `confirmed_by (TEXT)`, `confirmed_at (TIMESTAMP NULL)` |
| `certificates` | `id (PK)`, `volunteer_id (FK)`, `from_date (DATE)`, `to_date (DATE)`, `total_hours (NUMERIC)`, `issued_at (DATE)`, `cert_number (TEXT UNIQUE)`, `hash (TEXT — SHA-256 от содержимого)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Учёт участия** → ввод часов → ожидание подтверждения.  
- **Модуль 2. Сертификация** →  
  - фильтр: `confirmed = true`,  
  - агрегация: `SUM(hours_worked) GROUP BY volunteer_id, период`,  
  - генерация `certificates` (только после подтверждения всех записей в периоде).  
- → Контроль: нельзя выдать сертификат с `total_hours = 0`.

#### ✅ Для «отлично»  
- Генерация QR-кода в сертификате (библиотека `zxing`), содержащего `cert_number` и `hash`.  
- Проверка подлинности: ввести номер → сверить хеш.  
- График: «Динамика участия волонтёра» (часы по месяцам).

---

### 🔹 Тема 17. **Культурная карта для подростков**

#### 📌 Описание  
Агрегатор бесплатных/льготных мероприятий для подростков 12–18 лет: выставки, квесты, мастер-классы, кинопоказы. Система позволяет:  
- искать по возрасту, интересам, району,  
- записываться,  
- получать напоминания,  
- оставлять отзывы.

#### 🧩 Сущности  
- **Мероприятие**: название, описание, дата/время, место, организатор, возрастное ограничение (от/до), категория (искусство, IT, спорт).  
- **Участник** (подросток): ФИО, возраст, email/телефон (через родителя), школа.  
- **Запись**: мероприятие, участник, статус (ожидает/подтверждено/отменено).  
- **Напоминание**: дата отправки, способ (внутри системы), статус.  
- **Отзыв**: оценка (1–5), текст, дата.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `events` | `id (PK)`, `title (TEXT)`, `description (TEXT)`, `datetime (TIMESTAMP)`, `location (TEXT)`, `organizer (TEXT)`, `age_min (INT)`, `age_max (INT)`, `category (TEXT)`, `max_participants (INT)`, `current_participants (INT DEFAULT 0)` |
| `participants` | `id (PK)`, `fio (TEXT)`, `age (INT)`, `contact (TEXT)`, `school (TEXT)`, `parent_contact (TEXT)` |
| `registrations` | `id (PK)`, `event_id (FK)`, `participant_id (FK)`, `registered_at (TIMESTAMP)`, `status (ENUM: 'pending','confirmed','cancelled')` |
| `reminders` | `id (PK)`, `registration_id (FK)`, `scheduled_at (TIMESTAMP)`, `sent (BOOL DEFAULT false)`, `sent_at (TIMESTAMP NULL)` |
| `reviews` | `id (PK)`, `event_id (FK)`, `participant_id (FK)`, `rating (INT CHECK 1-5)`, `text (TEXT)`, `datetime (TIMESTAMP)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Поиск и запись** → фильтр по `age`, `category`, `datetime` → создание `registrations`.  
- **Модуль 2. Взаимодействие** →  
  - автоматическое создание `reminders` за 24 ч до события,  
  - после события — возможность оставить `reviews`.  
- → Валидация: нельзя записать 10-летнего на мероприятие «14+».

#### ✅ Для «отлично»  
- Автоматическое обновление `current_participants` при подтверждении регистрации.  
- Ограничение: `current_participants ≤ max_participants`.  
- Рейтинг мероприятия: среднее по `reviews.rating`.

---

### 🔹 Тема 18. **Обратная связь по школьному питанию**

#### 📌 Описание  
Ежедневный опрос учащихся/родителей по качеству питания: вкус, температура, соответствие меню, гигиена. Данные агрегируются в отчёты для администрации и поставщика.

#### 🧩 Сущности  
- **Меню на день**: дата, блюда (завтрак, обед, полдник).  
- **Блюдо**: название, категория (первое, второе, десерт), норма.  
- **Оценка**: блюдо, оценка (1–5), комментарий, дата, класс.  
- **Жалоба**: текст, фото (путь к файлу), статус (новая/в работе/закрыта), ответ.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `daily_menus` | `id (PK)`, `date (DATE UNIQUE)`, `breakfast (TEXT)`, `lunch_main (TEXT)`, `lunch_side (TEXT)`, `dessert (TEXT)`, `approved_by (TEXT)` |
| `dishes` | `id (PK)`, `name (TEXT)`, `category (TEXT)`, `standard_temp_min (INT)`, `standard_temp_max (INT)` |
| `menu_dishes` | `menu_id (FK)`, `dish_id (FK)`, `serving_order (INT)` *(многие-ко-многим)* |
| `feedback` | `id (PK)`, `dish_id (FK)`, `rating (INT CHECK 1-5)`, `comment (TEXT)`, `date (DATE)`, `class (TEXT)`, `anonymous (BOOL)` |
| `complaints` | `id (PK)`, `date (DATE)`, `class (TEXT)`, `text (TEXT)`, `photo_path (TEXT NULL)`, `status (ENUM: 'new','processing','resolved')`, `response (TEXT NULL)`, `responded_at (TIMESTAMP NULL)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Сбор обратной связи** → ввод оценок/жалоб → сохранение в `feedback` / `complaints`.  
- **Модуль 2. Аналитика и отчётность** →  
  - ежедневный отчёт: «Средняя оценка по блюдам»,  
  - еженедельный: «Топ-3 проблемных блюда»,  
  - по жалобам — уведомление завхозу при `status = 'new'`.  
- → Связь: жалоба может ссылаться на конкретное блюдо из `menu_dishes`.

#### ✅ Для «отлично»  
- Тепловая карта: «Оценки по дням недели» (таблица 5×7 с цветом ячейки).  
- Экспорт отчёта в Excel с графиками (Apache POI + `XSSFChart`).  
- Ограничение: одна оценка от одного класса на одно блюдо в день.

---

### 🔹 Тема 19. **Интеграция школьных мероприятий с родительским порталом**

#### 📌 Описание  
Родители получают уведомления о предстоящих мероприятиях (концерты, походы, олимпиады), подтверждают участие ребёнка, получают фотоотчёты. Система заменяет бумажные записки.

#### 🧩 Сущности  
- **Мероприятие**: название, дата, время, место, описание, ответственный педагог.  
- **Ребёнок**: ФИО, класс, ID в базе.  
- **Родитель**: ФИО, телефон, email, связь с ребёнком.  
- **Уведомление**: мероприятие, родитель, дата отправки, статус (отправлено/прочитано).  
- **Подтверждение**: родитель, мероприятие, выбор («участвует»/«не участвует»), комментарий, дата.  
- **Фотоотчёт**: мероприятие, ссылки на фото (пути), дата публикации.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `events` | `id (PK)`, `title (TEXT)`, `datetime (TIMESTAMP)`, `location (TEXT)`, `description (TEXT)`, `responsible_teacher (TEXT)` |
| `children` | `id (PK)`, `fio (TEXT)`, `class (TEXT)`, `birth_date (DATE)` |
| `parents` | `id (PK)`, `fio (TEXT)`, `phone (TEXT)`, `email (TEXT)`, `child_id (FK)` |
| `notifications` | `id (PK)`, `event_id (FK)`, `parent_id (FK)`, `sent_at (TIMESTAMP)`, `read_at (TIMESTAMP NULL)` |
| `confirmations` | `id (PK)`, `event_id (FK)`, `parent_id (FK)`, `participates (BOOL)`, `comment (TEXT)`, `confirmed_at (TIMESTAMP)` |
| `photo_reports` | `id (PK)`, `event_id (FK)`, `photos_json (TEXT — '[{"url":"...","desc":"..."}]')`, `published_at (TIMESTAMP)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Рассылка и подтверждение** →  
  - создание мероприятия → автоматическая генерация `notifications` для всех родителей в классе,  
  - при подтверждении → запись в `confirmations`.  
- **Модуль 2. Отчётность** → загрузка фото → публикация → уведомление родителей с `participates = true`.  
- → Отчёт: «Явка на мероприятие» = `COUNT(confirmed AND participates) / COUNT(parents)`.

#### ✅ Для «отлично»  
- Группировка подтверждений по классам в GUI.  
- Напоминание неподтвердившим за 3 дня до события.  
- Защита: `photos_json` не должен содержать полные пути к локальным файлам — только относительные (`/reports/event123/001.jpg`).

---

### 🔹 Тема 20. **Учёт дронов в STEM-проектах**

#### 📌 Описание  
В кружках робототехники/геоинформационных технологий используются дроны для съёмки, картографии, доставки макетов. Требуется строгий учёт:  
- кто, когда, зачем берёт дрон,  
- лог полётов (время, координаты, миссия),  
- техосмотр и ремонт,  
- инциденты (падения, потери связи).

#### 🧩 Сущности  
- **Дрон**: модель, серийный №, статус (исправен/в ремонте/списан), максимальное время полёта.  
- **Пилот** (студент/преподаватель): ФИО, допуск (да/нет), дата последнего инструктажа.  
- **Полёт**: дрон, пилот, дата/время, место, цель, плановая/фактическая длительность, статус.  
- **Техосмотр**: дрон, дата, инженер, замечания, допуск к полётам (да/нет).  
- **Инцидент**: полёт, тип (падение, потеря связи), описание, повреждения, виновник.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `drones` | `id (PK)`, `model (TEXT)`, `serial_number (TEXT UNIQUE)`, `max_flight_time_min (INT)`, `status (ENUM: 'ready','in_use','maintenance','scrapped')`, `last_inspection_date (DATE)` |
| `pilots` | `id (PK)`, `fio (TEXT)`, `role (TEXT)`, `certified (BOOL)`, `last_briefing_date (DATE)` |
| `flights` | `id (PK)`, `drone_id (FK)`, `pilot_id (FK)`, `start_time (TIMESTAMP)`, `end_time (TIMESTAMP NULL)`, `location (TEXT)`, `purpose (TEXT)`, `planned_duration_min (INT)`, `actual_duration_min (INT NULL)`, `status (ENUM: 'planned','in_progress','completed','aborted')` |
| `inspections` | `id (PK)`, `drone_id (FK)`, `inspection_date (DATE)`, `engineer_fio (TEXT)`, `notes (TEXT)`, `approved (BOOL)` |
| `incidents` | `id (PK)`, `flight_id (FK)`, `type (TEXT — 'crash','signal_loss','battery_low')`, `description (TEXT)`, `damage_description (TEXT)`, `responsible_pilot_id (FK)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Планирование и выполнение полётов** →  
  - перед стартом: проверка `drone.status = 'ready'` и `pilot.certified = true`,  
  - при старте: `drone.status = 'in_use'`,  
  - при завершении: `drone.status = 'ready'` (если нет инцидента).  
- **Модуль 2. Обслуживание и безопасность** →  
  - после инцидента → автоматическое создание задачи на осмотр,  
  - при `inspection.approved = false` → `drone.status = 'maintenance'`.  
- → Отчёт: «Наработка дронов (часы)», «Частота инцидентов по пилотам».

#### ✅ Для «отлично»  
- Валидация: `actual_duration_min ≤ max_flight_time_min + 10%`.  
- Предупреждение за 7 дней до срока инструктажа (`last_briefing_date + 1 year`).  
- Карта полётов (координаты → экспорт в KML для Google Earth).

---

### 🔹 Тема 21. **Аудиогиды по локальным достопримечательностям**

#### 📌 Описание  
В музее, парке или историческом квартале устанавливаются информационные точки (стенды с QR-кодами). Посетитель сканирует → слушает аудиорассказ. Система позволяет:  
- загружать и редактировать сценарии и аудиофайлы,  
- отслеживать популярность точек,  
- собирать отзывы,  
- формировать статистику для методистов.

#### 🧩 Сущности  
- **Точка**: номер, название, координаты (X, Y на карте/плане), категория (история, природа).  
- **Сценарий**: текст, продолжительность, автор, дата редакции.  
- **Аудиофайл**: путь к файлу (`.mp3`), язык, качество (HQ/обычное).  
- **Прослушивание**: точка, дата/время, продолжительность (фактическая), устройство (ID терминала).  
- **Отзыв**: оценка (1–5), текст, дата.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `points` | `id (PK)`, `number (INT)`, `title (TEXT)`, `x_coord (NUMERIC)`, `y_coord (NUMERIC)`, `category (TEXT)`, `is_active (BOOL DEFAULT true)` |
| `scripts` | `id (PK)`, `point_id (FK)`, `text (TEXT)`, `duration_sec (INT)`, `author (TEXT)`, `edited_at (TIMESTAMP)` |
| `audio_files` | `id (PK)`, `script_id (FK)`, `file_path (TEXT)`, `language (TEXT DEFAULT 'ru')`, `quality (ENUM: 'HQ','standard')`, `file_size_mb (NUMERIC)` |
| `play_logs` | `id (PK)`, `point_id (FK)`, `datetime (TIMESTAMP)`, `duration_sec (INT)`, `terminal_id (TEXT)`, `completed (BOOL)` |
| `reviews` | `id (PK)`, `point_id (FK)`, `rating (INT CHECK 1-5)`, `text (TEXT)`, `datetime (TIMESTAMP)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Управление контентом** → добавление точки → привязка сценария и аудиофайла → активация (`is_active = true`).  
- **Модуль 2. Аналитика** → на основе `play_logs` и `reviews`:  
  - «Популярность точки» = кол-во прослушиваний/неделю,  
  - «Качество контента» = средний рейтинг + доля незавершённых прослушиваний.  
- → Контроль: нельзя активировать точку без аудиофайла.

#### ✅ Для «отлично»  
- Валидация: `duration_sec` в `play_logs` ≤ `duration_sec` в `scripts`.  
- График: «Динамика прослушиваний по точкам» (линейный график).  
- Экспорт отчёта в CSV: `точка, прослушиваний, средняя оценка, % завершённых`.

---

### 🔹 Тема 22. **Архив устной истории**

#### 📌 Описание  
Школьный/краеведческий проект по сбору воспоминаний ветеранов, коренных жителей, старожилов. Система помогает:  
- регистрировать интервью,  
- хранить аудио/видео,  
- делать транскрибирование (вручную),  
- добавлять метки (персона, событие, место, дата),  
- осуществлять полнотекстовый поиск.

#### 🧩 Сущности  
- **Респондент**: ФИО, год рождения, место проживания, контакты, фото (путь).  
- **Интервью**: дата, место, интервьюер, длительность, формат (аудио/видео), путь к файлу.  
- **Транскрипт**: текст, синхронизация по времени (необязательно), статус (черновик/проверен).  
- **Метка**: тип («человек», «событие», «место», «дата»), значение («ВОВ», «р. Волга», «1941–1945»).  
- **Связь «интервью–метка»** — многие-ко-многим.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `respondents` | `id (PK)`, `fio (TEXT)`, `birth_year (INT)`, `residence (TEXT)`, `contact (TEXT)`, `photo_path (TEXT NULL)` |
| `interviews` | `id (PK)`, `respondent_id (FK)`, `interviewer (TEXT)`, `datetime (TIMESTAMP)`, `location (TEXT)`, `duration_min (INT)`, `media_type (ENUM: 'audio','video')`, `file_path (TEXT)` |
| `transcripts` | `id (PK)`, `interview_id (FK UNIQUE)`, `text (TEXT)`, `status (ENUM: 'draft','reviewed')`, `edited_at (TIMESTAMP)` |
| `tags` | `id (PK)`, `type (TEXT — 'person','event','place','date')`, `value (TEXT)`, `description (TEXT NULL)` |
| `interview_tags` | `interview_id (FK)`, `tag_id (FK)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Ввод и оцифровка** → добавление интервью → загрузка файла → ввод транскрипта.  
- **Модуль 2. Индексация и поиск** → привязка меток → полнотекстовый поиск по `transcripts.text` + фильтрация по `tags`.  
- → Запрос: «Все упоминания “пионерлагерь Заря”» → `SELECT * FROM interviews JOIN interview_tags...`.

#### ✅ Для «отлично»  
- Поддержка `tsvector` в PostgreSQL для полнотекстового поиска:  
  ```sql
  ALTER TABLE transcripts ADD COLUMN text_ts tsvector;
  CREATE INDEX idx_text_ts ON transcripts USING GIN(text_ts);
  UPDATE transcripts SET text_ts = to_tsvector('russian', text);
  ```
- Поиск с ранжированием: `ORDER BY ts_rank(text_ts, query) DESC`.  
- Экспорт фрагмента интервью + транскрипта в PDF.

---

### 🔹 Тема 23. **Управление STEM-проектами**

#### 📌 Описание  
Учебные проекты в STEM-направлениях (робототехника, биоинформатика, экомониторинг) требуют планирования: команды, этапы, ресурсы, защита. Система — аналог Trello/Jira, но для школьных условий.

#### 🧩 Сущности  
- **Проект**: название, описание, руководитель (учитель), статус.  
- **Участник** (студент): роль (разработчик, дизайнер, докладчик).  
- **Этап**: название, дедлайн, статус (не начат/в работе/готов).  
- **Задача**: описание, исполнитель, оценка сложности (1–5), статус.  
- **Ресурс**: оборудование, материалы, ПО.  
- **Защита**: дата, оценка, комментарий эксперта.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `projects` | `id (PK)`, `title (TEXT)`, `description (TEXT)`, `supervisor (TEXT)`, `status (TEXT)`, `created_at (DATE)` |
| `participants` | `id (PK)`, `project_id (FK)`, `student_fio (TEXT)`, `role (TEXT)` |
| `stages` | `id (PK)`, `project_id (FK)`, `name (TEXT)`, `deadline (DATE)`, `status (TEXT)` |
| `tasks` | `id (PK)`, `stage_id (FK)`, `description (TEXT)`, `assignee (TEXT)`, `complexity (INT CHECK 1-5)`, `status (TEXT)` |
| `resources` | `id (PK)`, `project_id (FK)`, `name (TEXT)`, `type (TEXT)`, `quantity (INT)` |
| `defenses` | `id (PK)`, `project_id (FK UNIQUE)`, `defense_date (DATE)`, `grade (INT CHECK 3-5)`, `expert_comment (TEXT)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Планирование** → создание проекта → этапы → задачи → распределение ресурсов.  
- **Модуль 2. Выполнение и оценка** → обновление статусов задач → фиксация защиты → расчёт итогового балла.  
- → Отчёт: «Нагрузка на студента» = сумма `complexity` по открытым задачам.

#### ✅ Для «отлично»  
- Гантт-диаграмма в GUI (JavaFX `BarChart` с горизонтальными полосами).  
- Оповещение за 2 дня до дедлайна этапа.  
- Ограничение: нельзя провести защиту, если есть задачи со статусом ≠ «готов».

---

### 🔹 Тема 24. **Контроль домашних заданий**

#### 📌 Описание  
Учитель задаёт ДЗ → ученики отмечают выполнение → учитель проверяет → ставит баллы. Аналог LMS, но локальный, без интернета.

#### 🧩 Сущности  
- **Ученик**, **Класс**, **Предмет**  
- **Домашнее задание**: описание, дедлайн, максимальный балл.  
- **Сдача ДЗ**: ученик, ДЗ, дата сдачи, статус (не сдано/сдано/проверено), файл (путь к `.pdf`/`.jpg`).  
- **Оценка**: балл, комментарий, дата проверки.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `students`, `classes`, `subjects` — стандартные |
| `homeworks` | `id (PK)`, `subject_id (FK)`, `class_id (FK)`, `title (TEXT)`, `description (TEXT)`, `deadline (DATE)`, `max_score (INT DEFAULT 10)` |
| `submissions` | `id (PK)`, `homework_id (FK)`, `student_id (FK)`, `submitted_at (TIMESTAMP NULL)`, `status (ENUM: 'pending','submitted','checked')`, `file_path (TEXT NULL)` |
| `grades` | `id (PK)`, `submission_id (FK UNIQUE)`, `score (INT)`, `comment (TEXT)`, `checked_at (TIMESTAMP)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Назначение и сдача** → учитель создаёт `homeworks` → ученики меняют `submissions.status`.  
- **Модуль 2. Проверка и анализ** → учитель вводит `grades` → обновляется `submissions.status = 'checked'`.  
- → Отчёт: «Прогресс по классу» = % сданных / % проверенных / средний балл.

#### ✅ Для «отлично»  
- Автоматический перевод `status = 'submitted'` при загрузке файла.  
- Цветовая индикация в таблице:  
  - 🔴 — не сдано и `deadline < TODAY`,  
  - 🟡 — сдано, но не проверено >3 дней,  
  - 🟢 — проверено.  
- Экспорт ведомости в Excel.

---

### 🔹 Тема 25. **Учёт научно-технического творчества учащихся**

#### 📌 Описание  
Фиксация участия в олимпиадах, конкурсах, конференциях: работа, авторы, награды, публикации. Нужно для портфолио и отчётности.

#### 🧩 Сущности  
- **Работа**: название, тип (проект, исследование), руководитель.  
- **Автор** (учащийся): ФИО, класс, школа.  
- **Конкурс/олимпиада**: название, уровень (школьный/городской/всероссийский), дата.  
- **Участие**: работа, конкурс, результат (приз/диплом/без награды), место.  
- **Публикация**: название журнала/сборника, страницы, DOI/URL.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `works` | `id (PK)`, `title (TEXT)`, `type (TEXT)`, `supervisor (TEXT)`, `created_year (INT)` |
| `authors` | `id (PK)`, `work_id (FK)`, `fio (TEXT)`, `class (TEXT)`, `school (TEXT)` |
| `events` | `id (PK)`, `name (TEXT)`, `level (ENUM: 'school','city','region','russia','international')`, `date (DATE)` |
| `participations` | `id (PK)`, `work_id (FK)`, `event_id (FK)`, `result (TEXT — 'winner','diploma','participant')`, `place (INT NULL)` |
| `publications` | `id (PK)`, `work_id (FK)`, `title (TEXT)`, `journal (TEXT)`, `pages (TEXT)`, `doi_or_url (TEXT)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Регистрация работ** → ввод работы + авторов.  
- **Модуль 2. Отслеживание успехов** → привязка к конкурсам и публикациям → рейтинг учащегося.  
- → Отчёт: «Лучшие работы по году» = сортировка по `level` и `result`.

#### ✅ Для «отлично»  
- Генерация списка публикаций в формате ГОСТ.  
- Диаграмма: «Распределение работ по направлениям».  
- Экспорт портфолио учащегося в PDF.

---

### 🔹 Тема 26. **Сервис бронирования учебных классов и оборудования**

#### 📌 Описание  
Преподаватели бронируют аудитории и ТСО (проекторы, VR-очки, наборы роботов) на определённое время. Система исключает пересечения и учитывает совместимость (например, VR-очки — только в классе с затемнением).

#### 🧩 Сущности  
- **Класс**: номер, вместимость, оборудование (проектор, доска, VR-ready).  
- **Оборудование**: наименование, инвентарный №, статус, совместимость (JSON-массив классов).  
- **Бронирование**: класс, оборудование (опционально), преподаватель, дата/время, цель.  
- **Подтверждение**: статус (ожидает/одобрено/отклонено), комментарий администратора.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `classrooms` | `id (PK)`, `number (TEXT)`, `capacity (INT)`, `has_projector (BOOL)`, `vr_ready (BOOL)`, `is_available (BOOL DEFAULT true)` |
| `equipment` | `id (PK)`, `name (TEXT)`, `inv_number (TEXT UNIQUE)`, `status (TEXT)`, `compatible_class_ids (INT[] — ID классов)` |
| `bookings` | `id (PK)`, `classroom_id (FK)`, `equipment_id (FK NULL)`, `teacher_fio (TEXT)`, `start_time (TIMESTAMP)`, `end_time (TIMESTAMP)`, `purpose (TEXT)`, `status (ENUM: 'pending','approved','rejected')`, `created_at (TIMESTAMP)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Заявка** → выбор класса → система предлагает совместимое оборудование.  
- **Модуль 2. Согласование** → администратор проверяет пересечения по времени и доступность → меняет `status`.  
- → Валидация:  
  - пересечение по времени — запрещено,  
  - бронирование VR-очков в `vr_ready = false` — невозможно.

#### ✅ Для «отлично»  
- Календарь-таблица «Загрузка классов» (цвет: свободно/занято/на согласовании).  
- Напоминание за 15 мин до начала (локальное уведомление в GUI).  
- Лог изменений статуса (`audit_log`).

---

### 🔹 Тема 27. **Цифровое портфолио педагога**

#### 📌 Описание  
Сбор достижений преподавателя: курсы, публикации, награды, отзывы студентов/коллег. Используется при аттестации.

#### 🧩 Сущности  
- **Педагог**: ФИО, стаж, должность, категория.  
- **Курс/семинар**: название, организатор, дата, часы, документ (путь к PDF).  
- **Публикация**: тип (статья, методичка), название, издание, год, страницы.  
- **Награда**: название, уровень, дата, приказ.  
- **Отзыв**: автор, текст, дата, оценка (необязательно).

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `teachers` | `id (PK)`, `fio (TEXT)`, `position (TEXT)`, `experience_years (INT)`, `category (TEXT)` |
| `courses` | `id (PK)`, `teacher_id (FK)`, `title (TEXT)`, `organizer (TEXT)`, `date (DATE)`, `hours (INT)`, `cert_path (TEXT)` |
| `publications` | `id (PK)`, `teacher_id (FK)`, `type (TEXT)`, `title (TEXT)`, `publisher (TEXT)`, `year (INT)`, `pages (TEXT)` |
| `awards` | `id (PK)`, `teacher_id (FK)`, `title (TEXT)`, `level (TEXT)`, `date (DATE)`, `order_number (TEXT)` |
| `reviews` | `id (PK)`, `teacher_id (FK)`, `author (TEXT)`, `text (TEXT)`, `date (DATE)`, `rating (INT NULL)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Ввод достижений** → добавление записей в подсистемы.  
- **Модуль 2. Формирование отчёта** → агрегация по годам → генерация PDF-портфолио (iText).  
- → Автоматический подсчёт:  
  - общее число публикаций,  
  - суммарные часы повышения квалификации.

#### ✅ Для «отлично»  
- Шаблоны отчётов: «Для аттестации», «Для конкурса “Учитель года”».  
- Сортировка по весу: награды уровня РФ > региональные > локальные.  
- Резервное копирование портфолио в ZIP (все PDF + база).

---

### 🔹 Тема 28. **Система отчётности по внеурочной деятельности**

#### 📌 Описание  
Классные руководители ведут отчёты: какие мероприятия проведены, кто участвовал, фото, результаты. Требуется структурированное хранение вместо Word-файлов.

#### 🧩 Сущности  
- **Мероприятие**: дата, название, тип (спортивное, патриотическое), место.  
- **Класс**, **Классный руководитель**  
- **Участие**: мероприятие, учащийся, роль (организатор, участник).  
- **Отчёт**: текст, фото (пути), дата составления, статус (черновик/согласован/принят).  
- **Руководитель (админ)**: кто проверил отчёт.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `events` | `id (PK)`, `date (DATE)`, `title (TEXT)`, `type (TEXT)`, `location (TEXT)`, `class_id (FK)` |
| `event_participants` | `event_id (FK)`, `student_fio (TEXT)`, `role (TEXT)` |
| `reports` | `id (PK)`, `event_id (FK UNIQUE)`, `text (TEXT)`, `photos_json (TEXT)`, `created_at (TIMESTAMP)`, `status (TEXT)`, `approved_by (TEXT NULL)`, `approved_at (TIMESTAMP NULL)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Проведение мероприятия** → регистрация события + участников.  
- **Модуль 2. Оформление отчёта** → связь `events → reports` → отправка на согласование.  
- → Контроль: отчёт не может быть «принят», если `photos_json` пуст.

#### ✅ Для «отлично»  
- Просмотр фото в GUI (Swing `JLabel` с `ImageIcon`).  
- Статистика: «Охват учащихся» = % учеников, участвовавших в ≥3 мероприятиях.  
- Шаблон отчёта с автоподстановкой: `Класс __ провёл мероприятие “__” ...`.

---

### 🔹 Тема 29. **Интерактивный календарь школьных событий**

#### 📌 Описание  
Единый календарь: уроки, каникулы, олимпиады, родительские собрания, ремонт. Поддержка уведомлений и экспорта.

#### 🧩 Сущности  
- **Событие**: название, дата/время, тип, организатор, описание.  
- **Тип события**: учёба, внеурочка, администрация, ремонт.  
- **Уведомление**: получатель (учитель/родитель), способ (внутри системы), статус.  
- **Экспорт**: формат (iCal, PDF), дата генерации, путь.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `event_types` | `id (PK)`, `name (TEXT)`, `color_hex (TEXT — '#FF5733')` |
| `events` | `id (PK)`, `title (TEXT)`, `datetime_start (TIMESTAMP)`, `datetime_end (TIMESTAMP)`, `type_id (FK)`, `organizer (TEXT)`, `description (TEXT)`, `location (TEXT)` |
| `notifications` | `id (PK)`, `event_id (FK)`, `recipient (TEXT)`, `scheduled_at (TIMESTAMP)`, `sent (BOOL)`, `sent_at (TIMESTAMP NULL)` |
| `exports` | `id (PK)`, `format (TEXT)`, `generated_at (TIMESTAMP)`, `file_path (TEXT)`, `period_start (DATE)`, `period_end (DATE)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Ведение календаря** → добавление/редактирование событий.  
- **Модуль 2. Взаимодействие** →  
  - авто-напоминания за 1 день,  
  - экспорт в iCal (`*.ics`) — можно открыть в Outlook/Google Calendar.  
- → Валидация: `datetime_end > datetime_start`.

#### ✅ Для «отлично»  
- Цветовая маркировка по типу (`color_hex` → фон в таблице).  
- Фильтры: по типу, организатору, дате.  
- Генерация `ics`-файла по стандарту RFC 5545.

---

### 🔹 Тема 30. **Модуль мониторинга энергопотребления в образовательной организации**

#### 📌 Описание  
Сбор показаний с общих и локальных счётчиков электроэнергии/тепла/воды → сравнение с нормативами → выявление перерасхода.

#### 🧩 Сущности  
- **Объект учёта**: здание, этаж, кабинет.  
- **Счётчик**: тип (электро/тепло/вода), номер, дата установки.  
- **Показание**: дата, значение, предыдущее значение, расход за период.  
- **Норматив**: объект, тип ресурса, норма (кВт·ч/м²·мес).  
- **Отклонение**: дата, ресурс, фактический расход, норма, % отклонения.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `objects` | `id (PK)`, `name (TEXT)`, `type (TEXT)`, `area_m2 (NUMERIC)` |
| `meters` | `id (PK)`, `object_id (FK)`, `type (TEXT)`, `serial_number (TEXT)`, `installed_at (DATE)` |
| `readings` | `id (PK)`, `meter_id (FK)`, `date (DATE)`, `value (NUMERIC)`, `prev_value (NUMERIC NULL)`, `consumption (NUMERIC GENERATED ALWAYS AS (value - prev_value) STORED)` |
| `norms` | `object_id (FK)`, `resource_type (TEXT)`, `norm_per_m2 (NUMERIC)`, `valid_from (DATE)` |
| `deviations` | `id (PK)`, `object_id (FK)`, `resource_type (TEXT)`, `month (DATE)`, `actual (NUMERIC)`, `normative (NUMERIC)`, `deviation_percent (NUMERIC GENERATED ALWAYS AS ((actual - normative) / normative * 100) STORED)` |

#### ⚙️ Интеграция модулей  
- **Модуль 1. Сбор данных** → ввод показаний → автоматический расчёт `consumption`.  
- **Модуль 2. Анализ** → на основе `norms` → расчёт `deviations` → уведомление при `|deviation_percent| > 20%`.  
- → Отчёт: «Топ-3 объекта с перерасходом».

#### ✅ Для «отлично»  
- График потребления по месяцам (линейный + горизонталь нормы).  
- Прогноз на год вперёд (линейная регрессия — можно упрощённо: средний рост).  
- Экспорт в Excel с условным форматированием.

---

### 🔹 Тема 31. **Система подбора наставников по интересам и компетенциям**

#### 📌 Описание  
Студент указывает интересы и цели → система подбирает наставника из базы (преподаватели, выпускники, работодатели).

#### 🧩 Сущности  
- **Наставник**: ФИО, компетенции, доступность, контакты.  
- **Студент**: ФИО, интересы, цели, текущий уровень.  
- **Компетенция**, **Интерес** — отдельные справочники.  
- **Запрос на наставничество**: студент, статус, дата.  
- **Подборка**: студент, наставник, совпадение (%), дата.  
- **Обратная связь**: оценка после встречи.

#### 📊 Таблицы *(аналогично Теме 5, но фокус на matching)*  
| Таблица | Ключевые поля |
|--------|----------------|
| `mentors`, `students`, `competencies`, `interests` | стандартные |
| `mentor_competencies`, `student_interests`, `student_competencies` | связи многие-ко-многим |
| `requests` | `student_id`, `status`, `created_at` |
| `matches` | `student_id`, `mentor_id`, `match_percent`, `created_at` |
| `feedbacks` | `match_id`, `rating`, `text`, `datetime` |

#### ⚙️ Интеграция  
- **Модуль 1. Поиск** → студент создаёт запрос → система генерирует `matches` по совпадению тегов.  
- **Модуль 2. Обратная связь** → после встречи → `feedbacks` → пересчёт рейтинга наставника.  
- → Алгоритм: `match_percent = (совпавшие интересы × 0.4 + компетенции × 0.6) / всего`.

#### ✅ Для «отлично»  
- Ручная коррекция весов (GUI: ползунки «интересы/компетенции»).  
- Чёрный список: студент может исключить наставника.  
- Статистика: «Средний рейтинг наставников по направлениям».

---

### 🔹 Тема 32. **Цифровая копилка школьных традиций и ритуалов**

#### 📌 Описание  
Сбор и сохранение школьных обычаев: «Посвящение в первоклассники», «Выпускной у Дерева желаний», «День самоуправления». Включает описание, фото, видео, участников.

#### 🧩 Сущности  
- **Традиция**: название, описание, периодичность (ежегодно/раз в 2 года).  
- **Проведение**: традиция, год, дата, организаторы, участники.  
- **Медиа**: тип (фото/видео), путь, описание, автор.  
- **Отзыв**: участник, текст, оценка.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `traditions` | `id (PK)`, `name (TEXT)`, `description (TEXT)`, `frequency (TEXT)`, `established_year (INT)` |
| `tradition_events` | `id (PK)`, `tradition_id (FK)`, `year (INT)`, `date (DATE)`, `organizers (TEXT)`, `participants_count (INT)` |
| `media_items` | `id (PK)`, `event_id (FK)`, `type (ENUM: 'photo','video')`, `file_path (TEXT)`, `description (TEXT)`, `author (TEXT)` |
| `tradition_feedback` | `id (PK)`, `event_id (FK)`, `participant (TEXT)`, `rating (INT)`, `text (TEXT)`, `year (INT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Архивация** → добавление традиции → привязка проведений и медиа.  
- **Модуль 2. Анализ** → рейтинги по годам → тренды: растёт/падает вовлечённость?  
- → Отчёт: «История традиции “Последний звонок” за 10 лет».

#### ✅ Для «отлично»  
- Хронологическая лента в GUI (по годам).  
- Поиск по ключевым словам в `description` и `text`.  
- Экспорт подборки «Лучшие традиции» в PDF.

---

### 🔹 Тема 33. **Платформа для проведения онлайн-олимпиад с интеграцией проверки и аналитики**

#### 📌 Описание  
Организация школьных олимпиад: загрузка задач → генерация билетов → таймер → автопроверка (тесты) / ручная (развёрнутые) → рейтинги.

#### 🧩 Сущности  
- **Олимпиада**: название, предмет, дата, длительность.  
- **Задача**: текст, тип (тест/развёрнутый), сложность, балл, правильный ответ (для тестов).  
- **Участник**: ФИО, школа, класс.  
- **Попытка**: участник, олимпиада, дата, старт/финиш, статус.  
- **Ответ**: задача, текст ответа, балл (авто/вручную), проверяющий.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `olympiads` | `id (PK)`, `title (TEXT)`, `subject (TEXT)`, `datetime (TIMESTAMP)`, `duration_min (INT)` |
| `tasks` | `id (PK)`, `olympiad_id (FK)`, `text (TEXT)`, `type (ENUM: 'test','open')`, `max_score (INT)`, `correct_answer (TEXT NULL)` |
| `participants` | `id (PK)`, `olympiad_id (FK)`, `fio (TEXT)`, `school (TEXT)`, `class (TEXT)` |
| `attempts` | `id (PK)`, `participant_id (FK)`, `start_time (TIMESTAMP)`, `end_time (TIMESTAMP NULL)`, `status (TEXT)` |
| `answers` | `id (PK)`, `attempt_id (FK)`, `task_id (FK)`, `answer_text (TEXT)`, `score (INT NULL)`, `checked_by (TEXT NULL)` |

#### ⚙️ Интеграция  
- **Модуль 1. Проведение** → участник начинает → таймер → ввод ответов.  
- **Модуль 2. Проверка** →  
  - для `type = 'test'` — авто-сравнение с `correct_answer`,  
  - для `type = 'open'` — интерфейс проверяющего.  
- → Автоматический рейтинг: `ORDER BY SUM(score) DESC`.

#### ✅ Для «отлично»  
- Таймер в GUI с обратным отсчётом.  
- Анализ: «Самые сложные задачи» = % решивших < 30%.  
- Генерация дипломов в PDF (с ФИО, местом, подписью).

---

### 🔹 Тема 34. **Система учёта и анализа посещаемости культурных учреждений учащимися**

#### 📌 Описание  
Школа организует посещение театров, музеев, выставок. Система фиксирует: кто пошёл, куда, когда, оставил ли отзыв.

#### 🧩 Сущности  
- **Учреждение**: название, тип, адрес, сайт.  
- **Группа**: класс, сопровождающий, дата посещения.  
- **Участник**: ФИО, роль (ученик/родитель/педагог).  
- **Отзыв**: оценка, текст, фото (путь).

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `institutions` | `id (PK)`, `name (TEXT)`, `type (TEXT)`, `address (TEXT)`, `website (TEXT)` |
| `visits` | `id (PK)`, `institution_id (FK)`, `class (TEXT)`, `teacher_fio (TEXT)`, `datetime (TIMESTAMP)` |
| `visit_participants` | `visit_id (FK)`, `fio (TEXT)`, `role (TEXT)` |
| `visit_feedback` | `visit_id (FK)`, `participant_fio (TEXT)`, `rating (INT)`, `text (TEXT)`, `photo_path (TEXT NULL)` |

#### ⚙️ Интеграция  
- **Модуль 1. Планирование** → создание `visits`.  
- **Модуль 2. Отчётность** → сбор `visit_feedback` → статистика по учреждениям.  
- → Отчёт: «Топ-5 учреждений по удовлетворённости».

#### ✅ Для «отлично»  
- Карта учреждений (координаты → `lat/lon` → экспорт в KML).  
- Сравнение: «Посещаемость vs. рейтинг» (диаграмма рассеяния).  
- Экспорт отчёта в Excel с диаграммой.

---

### 🔹 Тема 35. **Мобильное приложение для ориентирования на местности с AR-подсказками**  
> ⚠️ **Важно**: в ТЗ сказано **убрать мобильные приложения**, поэтому **реализуем как desktop-приложение с имитацией AR**:  
> - загрузка GPS-трека,  
> - отображение точек на карте,  
> - при «приближении» — показ подсказки (текст/аудио),  
> - лог прохождения.

#### 📌 Описание  
Для экскурсий и походов: на карте отмечены точки → при достижении → появляется информация. В desktop-версии — имитация по GPS-логу.

#### 🧩 Сущности  
- **Трек**: название, описание, файл трека (`.gpx`).  
- **Точка**: название, координаты, радиус активации (м), текст, аудиофайл.  
- **Сессия**: трек, дата, старт/финиш, пройденные точки.  
- **Лог**: точка, время активации, продолжительность.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `tracks` | `id (PK)`, `name (TEXT)`, `description (TEXT)`, `gpx_path (TEXT)` |
| `track_points` | `id (PK)`, `track_id (FK)`, `name (TEXT)`, `lat (NUMERIC)`, `lon (NUMERIC)`, `activation_radius_m (INT)`, `text (TEXT)`, `audio_path (TEXT NULL)` |
| `sessions` | `id (PK)`, `track_id (FK)`, `start_time (TIMESTAMP)`, `end_time (TIMESTAMP NULL)`, `completed (BOOL)` |
| `session_logs` | `id (PK)`, `session_id (FK)`, `point_id (FK)`, `activated_at (TIMESTAMP)`, `viewed_sec (INT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Подготовка маршрута** → загрузка GPX → добавление точек.  
- **Модуль 2. Прохождение** → имитация движения (слайдер времени) → активация точек при `расстояние ≤ радиус` → запись в `session_logs`.  
- → Отчёт: «Среднее время на точку».

#### ✅ Для «отлично»  
- Визуализация трека и точек в GUI (JavaFX `Canvas`).  
- Расчёт расстояния по формуле Haversine (на Java).  
- Экспорт сессии в `.gpx` с отметками времени активации.

---

### 🔹 Тема 36. **Система обратной связи по качеству образовательных онлайн-курсов**

#### 📌 Описание  
Студенты проходят онлайн-курсы (внутренние или внешние — Coursera, Stepik) → оставляют оценки по модулям → администрация анализирует.

#### 🧩 Сущности  
- **Курс**: название, платформа, URL, длительность.  
- **Модуль**: номер, название, продолжительность.  
- **Студент**  
- **Прохождение**: курс, студент, дата начала/окончания.  
- **Оценка модуля**: модуль, студент, оценка (1–5), сложность, полезность, комментарий.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `courses` | `id (PK)`, `title (TEXT)`, `platform (TEXT)`, `url (TEXT)`, `duration_hours (INT)` |
| `modules` | `id (PK)`, `course_id (FK)`, `number (INT)`, `title (TEXT)`, `duration_hours (INT)` |
| `enrollments` | `id (PK)`, `course_id (FK)`, `student_fio (TEXT)`, `start_date (DATE)`, `end_date (DATE NULL)` |
| `module_feedback` | `id (PK)`, `module_id (FK)`, `student_fio (TEXT)`, `rating (INT)`, `complexity (INT)`, `usefulness (INT)`, `comment (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Регистрация прохождения** → `enrollments`.  
- **Модуль 2. Сбор отзывов** → привязка к `modules` → агрегация.  
- → Отчёт: «Средняя полезность по курсам».

#### ✅ Для «отлично»  
- Диаграмма «паутина» по модулю: сложность vs полезность vs рейтинг.  
- Рекомендации: «Заменить модуль 3 — низкая полезность».  
- Экспорт в CSV для анализа в Python/R.

---

### 🔹 Тема 37. **Платформа для совместной разработки open-source учебных проектов**

#### 📌 Описание  
Студенты объединяются в команды для разработки ПО (калькуляторы, тренажёры). Система: регистрация проектов, распределение задач, ревью кода, релизы.

#### 🧩 Сущности  
- **Проект**: название, описание, репозиторий (URL), статус.  
- **Участник**: студент, роль (lead, dev, tester).  
- **Задача**: описание, исполнитель, статус, ветка в Git.  
- **Ревью**: задача, ревьюер, комментарии, решение (принять/доработать).  
- **Релиз**: версия, дата, изменения, артефакт (путь к `.jar`).

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `projects` | `id (PK)`, `name (TEXT)`, `description (TEXT)`, `repo_url (TEXT)`, `status (TEXT)` |
| `contributors` | `project_id (FK)`, `student_fio (TEXT)`, `role (TEXT)` |
| `tasks` | `id (PK)`, `project_id (FK)`, `description (TEXT)`, `assignee (TEXT)`, `branch (TEXT)`, `status (TEXT)` |
| `reviews` | `id (PK)`, `task_id (FK)`, `reviewer (TEXT)`, `comments (TEXT)`, `decision (ENUM: 'accept','revise')`, `reviewed_at (TIMESTAMP)` |
| `releases` | `id (PK)`, `project_id (FK)`, `version (TEXT)`, `release_date (DATE)`, `changelog (TEXT)`, `jar_path (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Разработка** → задачи → ревью → обновление статуса.  
- **Модуль 2. Релиз** → агрегация завершённых задач → создание `releases`.  
- → Контроль: нельзя сделать релиз, если есть задачи со статусом ≠ «done».

#### ✅ Для «отлично»  
- Интеграция с GitHub API (опционально: получение количества коммитов).  
- Диаграмма: «Вклад по участникам» (кол-во задач).  
- Генерация `CHANGELOG.md` из `releases.changelog`.

---

### 🔹 Тема 38. **Модуль интеграции школьной системы с порталом госуслуг**

#### 📌 Описание  
Автоматизация подачи заявлений: приём в 1 класс, перевод, выпуск. Система формирует заявку → отправляет (имитация) → отслеживает статус.

#### 🧩 Сущности  
- **Гражданин**: ФИО, СНИЛС, телефон, email.  
- **Ребёнок**: ФИО, дата рождения, документ.  
- **Заявка**: тип (приём/перевод), статус (черновик/отправлена/принята/отклонена), номер госуслуг.  
- **Документ**: название, путь к файлу (скан), тип (паспорт, свид-во).  
- **Журнал статусов**: дата, новый статус, комментарий.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `citizens` | `id (PK)`, `fio (TEXT)`, `snils (TEXT UNIQUE)`, `phone (TEXT)`, `email (TEXT)` |
| `children` | `id (PK)`, `citizen_id (FK)`, `fio (TEXT)`, `birth_date (DATE)`, `birth_cert (TEXT)` |
| `applications` | `id (PK)`, `citizen_id (FK)`, `child_id (FK)`, `type (TEXT)`, `status (TEXT)`, `gosuslugi_number (TEXT NULL)`, `submitted_at (TIMESTAMP NULL)` |
| `documents` | `id (PK)`, `application_id (FK)`, `name (TEXT)`, `file_path (TEXT)`, `type (TEXT)` |
| `status_log` | `id (PK)`, `application_id (FK)`, `new_status (TEXT)`, `changed_at (TIMESTAMP)`, `comment (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Формирование заявки** → гражданин + ребёнок + документы → черновик.  
- **Модуль 2. Отправка и мониторинг** → имитация отправки → обновление статуса → запись в `status_log`.  
- → Валидация: все обязательные документы должны быть загружены.

#### ✅ Для «отлично»  
- Формирование XML/JSON в формате, близком к реальному API госуслуг.  
- Проверка валидности СНИЛС (контрольная сумма).  
- Экспорт заявки в PDF по шаблону Минобрнауки.

---

### 🔹 Тема 39. **Система геймификации участия в школьном самоуправлении**

#### 📌 Описание  
Учащиеся получают баллы за участие: совет школы, волонтёрство, инициативы. На баллы — достижения, привилегии (выбор места в актовом).

#### 🧩 Сущности  
- **Учащийся**  
- **Действие**: название, стоимость в баллах, тип (совет, проект, помощь).  
- **Фиксация**: учащийся, действие, дата, подтверждено (да/нет).  
- **Достижение**: название, иконка (путь), порог баллов.  
- **Привилегия**: название, стоимость, описание.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `students` | `id (PK)`, `fio (TEXT)`, `class (TEXT)`, `total_points (INT DEFAULT 0)` |
| `actions` | `id (PK)`, `name (TEXT)`, `points (INT)`, `type (TEXT)` |
| `records` | `id (PK)`, `student_id (FK)`, `action_id (FK)`, `datetime (TIMESTAMP)`, `confirmed (BOOL)`, `confirmed_by (TEXT)` |
| `achievements` | `id (PK)`, `name (TEXT)`, `icon_path (TEXT)`, `required_points (INT)` |
| `privileges` | `id (PK)`, `name (TEXT)`, `cost_points (INT)`, `description (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Начисление** → фиксация действия → при подтверждении → `total_points += points`.  
- **Модуль 2. Награды** → проверка достижений (`total_points >= required`) → отображение иконок.  
- → Отчёт: «Топ-10 лидеров».

#### ✅ Для «отлично»  
- «Дерево достижений» в GUI (JavaFX `TreeTableView`).  
- Анимация при получении достижения.  
- История начислений с фильтром.

---

### 🔹 Тема 40. **Система учёта инклюзивных образовательных практик**

#### 📌 Описание  
Для учащихся с ОВЗ: индивидуальные программы, адаптация материалов, прогресс, взаимодействие с дефектологом.

#### 🧩 Сущности  
- **Учащийся**, **Диагноз** (тихое расстройство, ДЦП и т.д.)  
- **Адаптация**: тип (время, формат, техсредства), описание.  
- **ИПРА** (индивидуальная программа реабилитации и абилитации): дата, цели, сроки.  
- **Прогресс**: дата, параметр (речь, моторика), оценка, специалист.  
- **Ресурс**: адаптированный материал (путь к PDF), описание.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `students`, `diagnoses` | стандартные |
| `adaptations` | `id (PK)`, `student_id (FK)`, `type (TEXT)`, `description (TEXT)`, `approved_by (TEXT)` |
| `ipras` | `id (PK)`, `student_id (FK)`, `created_at (DATE)`, `goals (TEXT)`, `deadline (DATE)` |
| `progress_logs` | `id (PK)`, `student_id (FK)`, `parameter (TEXT)`, `score (INT)`, `specialist (TEXT)`, `date (DATE)` |
| `resources` | `id (PK)`, `student_id (FK)`, `name (TEXT)`, `file_path (TEXT)`, `description (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Планирование** → ИПРА + адаптации.  
- **Модуль 2. Мониторинг** → ввод прогресса → корректировка ИПРА.  
- → Отчёт: «Динамика параметра “речь” за полугодие».

#### ✅ Для «отлично»  
- График прогресса по параметрам.  
- Оповещение при приближении дедлайна ИПRA.  
- Экспорт ИПРА в PDF по форме ПМПК.

---

### 🔹 Тема 41. **Центр помощи бездомным животным**

#### 📌 Описание  
Приют для животных: учёт поступлений, ветеринарные процедуры, поиск хозяев, усыновление.

#### 🧩 Сущности  
- **Животное**: вид, кличка, возраст, пол, особые отметки, статус (в приюте/в семье/утрачено).  
- **Приют**, **Волонтёр**  
- **Ветеринарная карта**: прививки, стерилизация, болезни.  
- **Потенциальный хозяин**: ФИО, контакты, предпочтения.  
- **Усыновление**: животное, хозяин, дата, договор (путь к PDF).

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `animals` | `id (PK)`, `species (TEXT)`, `name (TEXT)`, `age_years (INT)`, `gender (TEXT)`, `description (TEXT)`, `status (TEXT)` |
| `vet_records` | `id (PK)`, `animal_id (FK)`, `procedure (TEXT)`, `date (DATE)`, `vet_name (TEXT)`, `notes (TEXT)` |
| `adopters` | `id (PK)`, `fio (TEXT)`, `phone (TEXT)`, `preferences (TEXT)` |
| `adoptions` | `id (PK)`, `animal_id (FK)`, `adopter_id (FK)`, `date (DATE)`, `contract_path (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Учёт** → поступление → веткарта.  
- **Модуль 2. Усыновление** → подбор → оформление → смена статуса животного.  
- → Контроль: нельзя усыновить без стерилизации (если требуется).

#### ✅ Для «отлично»  
- Фильтр по признакам: «кошка, до 2 лет, привита».  
- Напоминание о следующей прививке.  
- Статистика: «% возвратов» (животные, вернувшиеся в приют).

---

### 🔹 Тема 42. **Система отслеживания миграции диких животных (на основе датчиков и ГИС)**

#### 📌 Описание  
Животных метят GPS-ошейниками. Данные имитируются или вводятся вручную → строится маршрут → оповещение при выходе из зоны.

#### 🧩 Сущности  
- **Животное**: вид, кличка, дата метки.  
- **Метка (ошейник)**: ID, модель, дата установки.  
- **Координата**: время, широта, долгота, высота.  
- **Зона обитания**: полигон (JSON WKT), название.  
- **Событие**: тип (выход из зоны, остановка), дата, описание.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `animals` | `id (PK)`, `species (TEXT)`, `name (TEXT)`, `tagged_at (DATE)` |
| `tags` | `id (PK)`, `animal_id (FK)`, `tag_id (TEXT UNIQUE)`, `model (TEXT)`, `installed_at (DATE)` |
| `locations` | `id (PK)`, `tag_id (TEXT)`, `datetime (TIMESTAMP)`, `lat (NUMERIC)`, `lon (NUMERIC)`, `altitude (NUMERIC NULL)` |
| `zones` | `id (PK)`, `name (TEXT)`, `polygon_wkt (TEXT — 'POLYGON((...))')` |
| `alerts` | `id (PK)`, `animal_id (FK)`, `type (TEXT)`, `datetime (TIMESTAMP)`, `description (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Сбор данных** → ввод `locations`.  
- **Модуль 2. Анализ** → проверка попадания в зону (через `ST_Contains` или в Java) → создание `alerts`.  
- → Отчёт: «Маршрут животного за месяц».

#### ✅ Для «отлично»  
- Визуализация на карте (встроенный браузер JavaFX → OpenLayers).  
- Расчёт скорости перемещения.  
- Экспорт трека в GPX.

---

### 🔹 Тема 43. **Школа ментальной арифметики IQ007**

#### 📌 Описание  
Учебный центр: ученики, уровни, занятия, тесты, прогресс.  
Типичная CRM для образовательных услуг.

#### 🧩 Сущности  
- **Ученик**, **Родитель**, **Группа**  
- **Уровень**: S1, S2, ..., J5 — с описанием.  
- **Занятие**: дата, тема, домашнее задание.  
- **Тест**: уровень, дата, макс. балл.  
- **Результат теста**: ученик, тест, балл, время.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `students`, `parents`, `groups` | стандартные |
| `levels` | `id (PK)`, `code (TEXT)`, `name (TEXT)`, `description (TEXT)` |
| `lessons` | `id (PK)`, `group_id (FK)`, `level_id (FK)`, `date (DATE)`, `topic (TEXT)`, `homework (TEXT)` |
| `tests` | `id (PK)`, `level_id (FK)`, `date (DATE)`, `max_score (INT)` |
| `test_results` | `id (PK)`, `student_id (FK)`, `test_id (FK)`, `score (INT)`, `time_sec (INT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Обучение** → расписание занятий и тестов.  
- **Модуль 2. Оценка** → ввод результатов → расчёт прогресса (`score / max_score`).  
- → Отчёт: «Прогресс ученика по уровням».

#### ✅ Для «отлично»  
- Автоматический переход на следующий уровень при `score ≥ 80%`.  
- График скорости вычислений (`time_sec` по датам).  
- Уведомление родителям о результатах теста.

---

### 🔹 Тема 44. **Студия звукозаписи**

#### 📌 Описание  
Учёт сессий: артист, инженер, треки, оборудование, релизы.

#### 🧩 Сущности  
- **Артист**, **Инженер**  
- **Сессия**: дата, студия, цель (демо/альбом).  
- **Трек**: название, длительность, файл (путь), статус (сырой/обработан/мастер).  
- **Оборудование**: микрофон, интерфейс — для учёта износа.  
- **Релиз**: название, дата, треки, площадки (YouTube, Spotify).

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `artists`, `engineers` | стандартные |
| `sessions` | `id (PK)`, `artist_id (FK)`, `engineer_id (FK)`, `date (DATE)`, `purpose (TEXT)` |
| `tracks` | `id (PK)`, `session_id (FK)`, `title (TEXT)`, `duration_sec (INT)`, `file_path (TEXT)`, `status (TEXT)` |
| `session_equipment` | `session_id (FK)`, `equipment_name (TEXT)` |
| `releases` | `id (PK)`, `title (TEXT)`, `release_date (DATE)`, `tracks_json (TEXT)`, `platforms (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Запись** → сессия + треки.  
- **Модуль 2. Публикация** → сбор треков → релиз.  
- → Контроль: нельзя выпустить трек со статусом ≠ «мастер».

#### ✅ Для «отлично»  
- Прослушивание фрагментов в GUI (Java `AudioSystem`).  
- Анализ: «Среднее время на трек».  
- Экспорт списка треков в `.m3u`.

---

### 🔹 Тема 45. **Химчистка**

#### 📌 Описание  
Приём заказов: тип изделия, загрязнение, срок, стоимость. Учёт статуса.

#### 🧩 Сущности  
- **Заказ**: номер, клиент, дата приёма, срок выдачи.  
- **Изделие**: тип (пальто, костюм), материал, цвет.  
- **Загрязнение**: тип (жир, чернила), степень.  
- **Операция**: стирка, глажка, чистка — с ценой.  
- **Статус**: принят, в работе, готов, выдан.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `orders` | `id (PK)`, `order_number (TEXT UNIQUE)`, `client_name (TEXT)`, `phone (TEXT)`, `accept_date (DATE)`, `due_date (DATE)`, `status (TEXT)` |
| `items` | `id (PK)`, `order_id (FK)`, `type (TEXT)`, `material (TEXT)`, `color (TEXT)` |
| `stains` | `id (PK)`, `item_id (FK)`, `type (TEXT)`, `severity (INT)` |
| `operations` | `id (PK)`, `item_id (FK)`, `name (TEXT)`, `price (NUMERIC)`, `performed_at (DATE NULL)` |

#### ⚙️ Интеграция  
- **Модуль 1. Приём** → заказ + изделия + загрязнения → расчёт стоимости.  
- **Модуль 2. Исполнение** → отметка выполнения операций → смена статуса.  
- → Валидация: нельзя выдать, если не все операции выполнены.

#### ✅ Для «отлично»  
- Автоматический расчёт стоимости по прайсу.  
- Напоминание о просроченных заказах.  
- Статистика: «Топ-5 типов загрязнений».

---

### 🔹 Тема 46. **Мастерская по ремонту одежды**

#### 📌 Описание  
Аналогично химчистке, но с акцентом на ремонт: порезы, замена фурнитуры, пошив.

#### 🧩 Сущности  
- **Заказ**, **Клиент**  
- **Изделие**: тип, размер, дефект (дыра, молния, подкладка).  
- **Ремонт**: операция (зашить, заменить), материал (нитки, ткань), стоимость.  
- **Мастер**

#### 📊 Таблицы *(по аналогии с 45)*  
Ключевые отличия:  
- `defects` вместо `stains`,  
- `repairs` вместо `operations`,  
- `master_id` в заказе.

#### ✅ Для «отлично»  
- Расчёт стоимости с учётом сложности дефекта.  
- Фото до/после (пути в БД).

---

### 🔹 Тема 47. **Производство светильников «Светлячок» (подсистема работы с продукцией)**

#### 📌 Описание  
Учёт готовых изделий: модель, комплектация, остатки, отгрузка.

#### 🧩 Сущности  
- **Модель светильника**: артикул, название, описание.  
- **Комплектующие**: корпус, патрон, провод — с нормой расхода.  
- **Партия**: дата, количество, брак.  
- **Склад**: остатки по моделям.  
- **Отгрузка**: клиент, дата, позиции.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `products` | `id (PK)`, `article (TEXT UNIQUE)`, `name (TEXT)` |
| `components` | `id (PK)`, `name (TEXT)` |
| `product_components` | `product_id (FK)`, `component_id (FK)`, `quantity (NUMERIC)` |
| `batches` | `id (PK)`, `product_id (FK)`, `produce_date (DATE)`, `quantity (INT)`, `defect_count (INT)` |
| `stock` | `product_id (PK)`, `current_quantity (INT)` |
| `shipments`, `shipment_items` | стандартные |

#### ⚙️ Интеграция  
- Производство → `batches` → обновление `stock`.  
- Отгрузка → проверка остатков → списание.

#### ✅ Для «отлично»  
- Расчёт себестоимости по комплектующим.  
- График брака по партиям.

---

### 🔹 Тема 48. **Техническое обслуживание станков**

#### 📌 Описание  
Планово-предупредительное обслуживание: график, исполнители, акты.

#### 🧩 Сущности  
- **Станок**: модель, инвентарный №, дата ввода.  
- **Вид ТО**: ежедневное, ежемесячное, капитальное.  
- **График ТО**: станок, вид, периодичность (дней), дата следующего.  
- **Акт ТО**: дата, инженер, замечания, подпись.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `machines` | `id (PK)`, `model (TEXT)`, `inv_number (TEXT)`, `commissioned_at (DATE)` |
| `maintenance_types` | `id (PK)`, `name (TEXT)`, `interval_days (INT)` |
| `maintenance_schedule` | `machine_id (FK)`, `type_id (FK)`, `next_due (DATE)`, `last_done (DATE NULL)` |
| `maintenance_acts` | `id (PK)`, `machine_id (FK)`, `type_id (FK)`, `date (DATE)`, `engineer (TEXT)`, `notes (TEXT)`, `signed (BOOL)` |

#### ⚙️ Интеграция  
- После акта → обновление `next_due = last_done + interval_days`.  
- Отчёт: «Просроченные ТО».

#### ✅ Для «отлично»  
- Календарь ТО с цветовой индикацией.  
- Уведомление за 3 дня до срока.

---

### 🔹 Тема 49. **Учёт личных автомобилей**

#### 📌 Описание  
Персональный учёт для владельца: ТО, страховка, штрафы, расходы.

#### 🧩 Сущности  
- **Автомобиль**: марка, модель, ГРЗ, VIN, дата покупки.  
- **ТО**: дата, пробег, работы, стоимость.  
- **Страховка**: тип (ОСАГО/КАСКО), дата окончания, страховая.  
- **Штраф**: дата, сумма, оплачен (да/нет), номер постановления.  
- **Расходы**: топливо, запчасти — с категорией.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `cars` | `id (PK)`, `brand (TEXT)`, `model (TEXT)`, `plate (TEXT)`, `vin (TEXT)`, `purchase_date (DATE)` |
| `tos` | `id (PK)`, `car_id (FK)`, `date (DATE)`, `mileage_km (INT)`, `works (TEXT)`, `cost (NUMERIC)` |
| `insurances` | `id (PK)`, `car_id (FK)`, `type (TEXT)`, `start_date (DATE)`, `end_date (DATE)`, `company (TEXT)` |
| `fines` | `id (PK)`, `car_id (FK)`, `date (DATE)`, `amount (NUMERIC)`, `paid (BOOL)`, `resolution_number (TEXT)` |
| `expenses` | `id (PK)`, `car_id (FK)`, `date (DATE)`, `category (TEXT)`, `amount (NUMERIC)`, `description (TEXT)` |

#### ⚙️ Интеграция  
- **Модуль 1. Учёт** → ввод данных.  
- **Модуль 2. Напоминания** → страховка, ТО по пробегу/времени.  
- → Отчёт: «Расходы по месяцам».

#### ✅ Для «отлично»  
- График расходов по категориям.  
- Расчёт стоимости владения на 100 км.

---

### 🔹 Тема 50. **Конструктор экзаменационных билетов**

#### 📌 Описание  
Преподаватель создаёт вопросы → распределяет по темам и сложности → генерирует билеты → анализирует использование.

#### 🧩 Сущности  
- **Предмет**, **Тема**, **Вопрос** (текст, тип, сложность 1–3).  
- **Билет**: номер, предмет, вопросы (3–5 шт).  
- **Использование**: билет, дата, группа, результаты.

#### 📊 Таблицы

| Таблица | Поля |
|--------|------|
| `subjects`, `topics` | стандартные |
| `questions` | `id (PK)`, `subject_id (FK)`, `topic_id (FK)`, `text (TEXT)`, `type (TEXT)`, `difficulty (INT)`, `answer (TEXT)` |
| `tickets` | `id (PK)`, `subject_id (FK)`, `number (INT)` |
| `ticket_questions` | `ticket_id (FK)`, `question_id (FK)`, `order_num (INT)` |
| `usages` | `id (PK)`, `ticket_id (FK)`, `date (DATE)`, `group_name (TEXT)`, `avg_score (NUMERIC)` |

#### ⚙️ Интеграция  
- **Модуль 1. Создание** → вопросы → билеты (баланс сложности).  
- **Модуль 2. Анализ** → статистика по вопросам: сколько раз использован, средний балл.  
- → Рекомендация: «Вопрос №123 — низкий балл — переписать».

#### ✅ Для «отлично»  
- Автогенерация билетов с равномерным распределением сложности.  
- Экспорт билета в PDF с QR-кодом (номер билета).  
- Анализ корреляции: сложность vs. результат.

---
